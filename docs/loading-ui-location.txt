src/app/components/InboxGroupMaker.tsx:12: const [open,setOpen]=useState(false),[profiles,setProfiles]=useState<Profile[]>([]),[selected,setSelected]=useState<string[]>([]),[title,setTitle]=useState(''),[query,setQuery]=useState(''),[loading,setLoading]=useState(false),[saving,setSaving]=useState(false),[uploadingImage,setUploadingImage]=useState(false),[error,setError]=useState<string|null>(null),[groupImage,setGroupImage]=useState<string|null>(null);
src/app/components/InboxGroupMaker.tsx:18: const close=()=>{if(saving||uploadingImage)return;setOpen(false);setSelected([]);setTitle('');setQuery('');setGroupImage(null);setError(null)};
src/app/components/InboxGroupMaker.tsx:20: const uploadGroupImage=async(file:File)=>{setError(null);if(!file.type.startsWith('image/')){setError('Please choose an image file.');return}if(file.size>MAX_IMAGE_SIZE){setError('Group image must be 10 MB or smaller.');return}setUploadingImage(true);try{const ext=(file.name.split('.').pop()||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'')||'jpg';const path=`${profileId}/groups/${crypto.randomUUID()}.${ext}`;const up=await supabase.storage.from(BUCKET).upload(path,file,{contentType:file.type,cacheControl:'31536000',upsert:false});if(up.error)throw up.error;const {data}=supabase.storage.from(BUCKET).getPublicUrl(path);setGroupImage(data.publicUrl)}catch(e){setError(e instanceof Error?e.message:'Could not upload group image.')}finally{setUploadingImage(false);if(imageInput.current)imageInput.current.value=''}};
src/app/components/InboxGroupMaker.tsx:23: return createPortal(<div className="ws-group-overlay" role="dialog" aria-modal="true" aria-label="Create group" onMouseDown={e=>{if(e.target===e.currentTarget)close()}}><div className="ws-group-modal"><div className="ws-group-head"><div><span className="ws-group-kicker">WORK SOCIAL</span><h2>Create a group</h2><p>Add multiple people and start one shared conversation.</p></div><button type="button" className="ws-group-close" onClick={close} aria-label="Close">×</button></div><div className="ws-group-photo-row"><button type="button" className="ws-group-photo" onClick={chooseImage} disabled={uploadingImage} aria-label="Add group photo">{groupImage?<img src={groupImage} alt="Group preview"/>:<span>📷</span>}</button><div><strong>{groupImage?'Group photo added':'Add a group photo'}</strong><small>{uploadingImage?'Uploading…':'Optional · JPG, PNG, WebP or GIF · max 10 MB'}</small>{groupImage&&<button type="button" className="ws-group-remove-photo" onClick={()=>setGroupImage(null)}>Remove photo</button>}</div></div><input ref={imageInput} type="file" accept="image/jpeg,image/png,image/webp,image/gif" hidden onChange={e=>{const file=e.target.files?.[0];if(file)void uploadGroupImage(file)}}/><input className="ws-group-title" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Group name" maxLength={80}/><div className="ws-group-search-wrap"><span>⌕</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search people to add..."/></div><div className="ws-group-selected">{selected.length?selected.map(id=>{const p=profiles.find(x=>x.id===id);return <button key={id} type="button" onClick={()=>toggle(id)} className="ws-group-chip">{p?.avatar_url?<img src={p.avatar_url} alt=""/>:<span>👤</span>}{label(p)} ×</button>}):<span>{loading?'Loading people…':'Select 2 or more people'}</span>}</div><div className="ws-group-list">{visible.map(p=>{const checked=selected.includes(p.id);return <button key={p.id} type="button" className={`ws-group-person${checked?' is-selected':''}`} onClick={()=>toggle(p.id)}><span className="ws-group-avatar">{p.avatar_url?<img src={p.avatar_url} alt=""/>:'👤'}</span><span><strong>{label(p)}</strong><small>{p.username?`@${p.username}`:' '}</small></span><span className="ws-group-check">{checked?'✓':''}</span></button>})}</div>{error&&<p className="ws-group-error" role="alert">{error}</p>}<button type="button" className="ws-group-create" disabled={saving||uploadingImage||selected.length<2||!title.trim()} onClick={()=>void create()}>{saving?'Creating group…':`Create Group${selected.length?` · ${selected.length+1} members`:''}`}</button></div><style>{`.ws-premium-group-trigger{position:relative!important;overflow:hidden!important;border:1px solid rgba(125,211,252,.42)!important;border-radius:15px!important;background:linear-gradient(145deg,rgba(255,255,255,.99),rgba(239,246,255,.96))!important;color:#4f46e5!important;font-weight:900!important;box-shadow:inset 0 1px 0 #fff,inset 0 -2px 4px rgba(79,70,229,.06),0 5px 14px rgba(79,70,229,.12),0 0 20px rgba(34,193,220,.09)!important;transition:transform .18s ease,box-shadow .18s ease,border-color .18s ease!important}.ws-premium-group-trigger::after{content:'';position:absolute;inset:1px;border-radius:13px;background:linear-gradient(120deg,transparent 20%,rgba(255,255,255,.65) 48%,transparent 72%);transform:translateX(-130%);transition:transform .45s ease;pointer-events:none}.ws-premium-group-trigger:hover{transform:translateY(-1px)!important;border-color:rgba(109,93,252,.55)!important;box-shadow:inset 0 1px 0 #fff,0 8px 20px rgba(79,70,229,.16),0 0 24px rgba(34,193,220,.12)!important}.ws-premium-group-trigger:hover::after{transform:translateX(130%)}.ws-premium-group-trigger:active{transform:translateY(1px)!important}.ws-group-overlay{position:fixed;inset:0;z-index:2000;display:grid;place-items:center;padding:18px;background:rgba(15,23,42,.42);backdrop-filter:blur(8px);-webkit-backdrop-filter:blur(8px)}.ws-group-modal{width:min(520px,calc(100vw - 28px));max-height:min(720px,calc(100dvh - 28px));display:flex;flex-direction:column;overflow:hidden;border:1px solid rgba(255,255,255,.72);border-radius:24px;background:linear-gradient(145deg,rgba(255,255,255,.99),rgba(244,247,255,.98));box-shadow:0 28px 80px rgba(15,23,42,.28),inset 0 1px 0 #fff}.ws-group-head{display:flex;justify-content:space-between;gap:12px;padding:20px 20px 14px}.ws-group-kicker{font-size:9px;font-weight:900;letter-spacing:.16em;color:#6d5dfc}.ws-group-head h2{margin:3px 0 3px;font-size:22px;font-weight:900;color:#111827}.ws-group-head p{margin:0;color:#667085;font-size:12px}.ws-group-close{width:38px;height:38px;flex:0 0 38px;border:1px solid rgba(99,102,241,.14);border-radius:50%;background:#fff;font-size:24px;color:#475467;box-shadow:0 5px 12px rgba(15,23,42,.06)}.ws-group-photo-row{display:flex;align-items:center;gap:12px;margin:0 20px 11px;padding:10px;border:1px solid rgba(99,102,241,.12);border-radius:16px;background:linear-gradient(145deg,rgba(255,255,255,.9),rgba(239,246,255,.72));box-shadow:inset 0 1px 0 #fff,0 5px 14px rgba(79,70,229,.05)}.ws-group-photo{width:58px;height:58px;flex:0 0 58px;display:grid;place-items:center;border:2px solid rgba(109,93,252,.22);border-radius:18px;overflow:hidden;background:linear-gradient(135deg,#eef2ff,#ecfeff);font-size:24px;box-shadow:0 5px 14px rgba(79,70,229,.12);cursor:pointer}.ws-group-photo img{width:100%;height:100%;object-fit:cover;display:block}.ws-group-photo-row strong,.ws-group-photo-row small{display:block}.ws-group-photo-row strong{font-size:12px;color:#344054}.ws-group-photo-row small{margin-top:3px;color:#98a2b3;font-size:10px}.ws-group-remove-photo{margin-top:5px;padding:0;border:0;background:transparent;color:#6d5dfc;font-size:10px;font-weight:800}.ws-group-title,.ws-group-search-wrap input{margin:0 20px 9px;box-sizing:border-box;border:1px solid rgba(99,102,241,.16);border-radius:14px;background:rgba(255,255,255,.9);padding:11px 13px;outline:none;box-shadow:inset 0 1px 2px rgba(15,23,42,.03),0 4px 12px rgba(79,70,229,.04)}.ws-group-search-wrap{display:flex;align-items:center;margin:0 20px 8px;border:1px solid rgba(99,102,241,.14);border-radius:14px;background:#fff;box-shadow:inset 0 1px 2px rgba(15,23,42,.03)}.ws-group-search-wrap span{padding-left:12px;font-size:20px;color:#6d5dfc}.ws-group-search-wrap input{flex:1;margin:0;border:0;box-shadow:none}.ws-group-selected{display:flex;flex-wrap:wrap;gap:6px;min-height:36px;margin:0 20px 8px;padding:4px 0;color:#98a2b3;font-size:11px}.ws-group-chip{display:inline-flex;align-items:center;gap:5px;border:1px solid rgba(109,93,252,.16);border-radius:999px;padding:5px 9px;background:linear-gradient(145deg,#fff,#f3f5ff);color:#344054;font-weight:750}.ws-group-chip img,.ws-group-avatar img{width:28px;height:28px;border-radius:50%;object-fit:cover}.ws-group-list{overflow:auto;min-height:120px;padding:0 12px 8px}.ws-group-person{width:100%;display:flex;align-items:center;gap:10px;padding:9px 8px;border:0;border-radius:14px;background:transparent;text-align:left}.ws-group-person:hover,.ws-group-person.is-selected{background:linear-gradient(90deg,rgba(109,93,252,.08),rgba(34,193,220,.05))}.ws-group-avatar{width:36px;height:36px;flex:0 0 36px;display:grid;place-items:center;border-radius:50%;background:#eef2ff;overflow:hidden}.ws-group-person span:nth-child(2){min-width:0;flex:1}.ws-group-person strong,.ws-group-person small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.ws-group-person small{margin-top:2px;color:#98a2b3;font-size:10px}.ws-group-check{width:24px;height:24px;display:grid;place-items:center;border:1px solid rgba(99,102,241,.18);border-radius:8px;color:#fff;font-weight:900}.is-selected .ws-group-check{background:linear-gradient(135deg,#6d5dfc,#22c1dc);border-color:transparent}.ws-group-error{margin:4px 20px 8px;color:#b42318;font-size:12px;font-weight:700}.ws-group-create{margin:8px 20px 20px;padding:12px 16px;border:0;border-radius:15px;background:linear-gradient(135deg,#6d5dfc,#22c1dc);color:#fff;font-weight:900;box-shadow:0 9px 24px rgba(79,70,229,.22)}.ws-group-create:disabled{opacity:.45;box-shadow:none}@media(max-width:600px){.ws-group-overlay{padding:10px}.ws-group-modal{width:100%;max-height:calc(100dvh - 20px);border-radius:20px}}`}</style></div>,document.body);
src/app/components/InboxGroupMenu.tsx:39: {mode==='avatar'&&<div style={{padding:'0 20px 20px'}}><button type="button" disabled={saving} onClick={()=>inputRef.current?.click()} style={primary}>{saving?'Uploading…':'Choose Group Avatar'}</button><p style={{fontSize:10,color:'#98a2b3'}}>JPG, PNG, WebP or GIF · max 10 MB</p><input ref={inputRef} type="file" hidden accept="image/jpeg,image/png,image/webp,image/gif" onChange={e=>{const f=e.target.files?.[0];if(f)void upload(f)}}/></div>}
src/app/pages/BlockedUsersPage.tsx:8:  const [loading, setLoading] = useState(true);
src/app/pages/BlockedUsersPage.tsx:35:    {loading && <p>Loading blocked users…</p>}
src/app/pages/BlockedUsersPage.tsx:37:    {!loading && !users.length && <section className="foundation-card"><p>No blocked users.</p></section>}
src/app/pages/BlockedUsersPage.tsx:38:    {!loading && users.length > 0 && <section style={{ display: 'grid', gap: 10 }}>
src/app/pages/FriendsPage.tsx:48:  const [loading, setLoading] = useState(true);
src/app/pages/FriendsPage.tsx:85:        {loading && <section style={styles.section}><div style={styles.empty}>Loading your social circle…</div></section>}
src/app/pages/FriendsPage.tsx:86:        {!loading && requests.length > 0 && <section style={styles.section}><h2 style={styles.sectionTitle}><span>🤝 Friend Requests</span><span style={{ fontSize: 12, padding: '5px 9px', borderRadius: 999, background: '#eeeaff', color: '#5b42c7' }}>{requests.length}</span></h2>{requests.map((request) => { const sender = profiles.find((p) => p.id === request.sender_id); return <article key={request.id} style={styles.row}><div style={styles.identity}>{sender?.avatar_url ? <img src={sender.avatar_url} alt="" style={styles.avatar} /> : <div aria-hidden="true" style={styles.avatarFallback}>👤</div>}<div style={{ flex: 1, minWidth: 0 }}><strong style={{ display: 'block', fontSize: 15 }}>{sender?.display_name ?? 'User'}</strong><span style={{ color: '#85899a', fontSize: 12 }}>wants to connect with you</span></div></div><div style={styles.actions}><button type="button" onClick={() => void respond(request, 'accepted')} style={{ ...styles.buttonBase, ...styles.accept }}>Accept</button><button type="button" onClick={() => void respond(request, 'rejected')} style={{ ...styles.buttonBase, ...styles.reject }}>Reject</button></div></article>; })}</section>}
src/app/pages/FriendsPage.tsx:87:        {!loading && <section style={styles.section}><h2 style={styles.sectionTitle}><span>{search.trim() ? '🔎 Search results' : '✨ People'}</span><span style={{ fontSize: 12, color: '#888c9d' }}>{filtered.length} people</span></h2>{filtered.map((p) => { const isFriend = friendIds.has(p.id); const isPending = pendingIds.has(p.id); const isFollowing = followingIds.has(p.id); return <article key={p.id} style={styles.row}><div style={styles.identity}><button type="button" onClick={() => navigate(`/profile/${encodeURIComponent(p.id)}`)} aria-label={`Open ${p.display_name ?? 'User'} profile`} style={{ border: 0, background: 'transparent', padding: 0, cursor: 'pointer', flexShrink: 0 }}>{p.avatar_url ? <img src={p.avatar_url} alt="" style={styles.avatar} /> : <div aria-hidden="true" style={styles.avatarFallback}>👤</div>}</button><button type="button" onClick={() => navigate(`/profile/${encodeURIComponent(p.id)}`)} style={styles.nameButton}><strong style={{ display: 'block', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{p.display_name ?? 'User'}</strong><span style={{ display: 'block', marginTop: 3, color: '#8a8e9f', fontSize: 12 }}>{isFriend ? 'Friend' : isFollowing ? 'Following you' : 'Work Social member'}</span></button></div><div style={styles.actions}><button type="button" onClick={() => void toggleFollow(p.id)} style={{ ...styles.buttonBase, ...(isFollowing ? styles.pending : styles.follow) }}>{isFollowing ? 'Following' : 'Follow'}</button><button type="button" onClick={() => isPending ? void cancelRequest(p.id) : void sendRequest(p.id)} disabled={isFriend} style={{ ...styles.buttonBase, ...(isFriend ? styles.friend : isPending ? styles.pending : styles.add), opacity: isFriend ? 1 : undefined }}>{isFriend ? '✓ Friends' : isPending ? 'Cancel' : 'Add friend'}</button></div></article>; })}{!filtered.length && <div style={styles.empty}><div style={{ fontSize: 30, marginBottom: 8 }}>🫶</div><strong>No people found</strong><div style={{ marginTop: 4, fontSize: 13 }}>Try a different name.</div></div>}</section>}
src/app/pages/InboxPage.tsx:37: const [selectedId,setSelectedId]=useState<string|null>(()=>new URLSearchParams(window.location.search).get('conversation')),[draft,setDraft]=useState(''),[search,setSearch]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null),[actionId,setActionId]=useState<string|null>(null),[actionPosition,setActionPosition]=useState<ActionPosition|null>(null),[deleteId,setDeleteId]=useState<string|null>(null),[editingId,setEditingId]=useState<string|null>(null),[replyId,setReplyId]=useState<string|null>(null),[mobile,setMobile]=useState(()=>window.innerWidth<768),[recording,setRecording]=useState(false),[recordingMs,setRecordingMs]=useState(0),[uploading,setUploading]=useState(false);
src/app/pages/InboxPage.tsx:78: const sendFiles=async(files:FileList|null)=>{if(!files||!selectedId)return;setUploading(true);setError(null);try{for(const file of Array.from(files)){if(!file.type.startsWith('image/')&&!file.type.startsWith('video/'))throw new Error('Only images and videos can be sent.');if(file.size>25*1024*1024)throw new Error('Each image/video must be 25 MB or smaller.');const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,'_'),path=`${selectedId}/${profileId}/${crypto.randomUUID()}-${safe}`,up=await supabase.storage.from('chat-media').upload(path,file,{contentType:file.type,cacheControl:'3600',upsert:false});if(up.error)throw up.error;const p:any={conversation_id:selectedId,content:'',message_type:file.type.startsWith('image/')?'image':'video',media_path:path,media_mime:file.type,media_name:file.name,media_size:file.size};if(replyId)p.reply_to_message_id=replyId;const ins=await supabase.from('messages').insert(p);if(ins.error)throw ins.error}setReplyId(null);await load()}catch(e){setError(e instanceof Error?e.message:'Could not send media.')}finally{setUploading(false);if(fileInput.current)fileInput.current.value=''}};
src/app/pages/InboxPage.tsx:79: const startRecording=async()=>{if(!selectedId||recording)return;try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});mediaStream.current=stream;const mime=MediaRecorder.isTypeSupported('audio/webm;codecs=opus')?'audio/webm;codecs=opus':'audio/webm',r=new MediaRecorder(stream,{mimeType:mime});audioChunks.current=[];recordingStarted.current=Date.now();r.ondataavailable=e=>{if(e.data.size)audioChunks.current.push(e.data)};r.onstop=async()=>{const blob=new Blob(audioChunks.current,{type:r.mimeType}),durationMs=Math.min(Date.now()-recordingStarted.current,120000);try{setUploading(true);const path=`${selectedId}/${profileId}/${crypto.randomUUID()}.webm`;const up=await supabase.storage.from('chat-media').upload(path,blob,{contentType:r.mimeType,cacheControl:'3600',upsert:false});if(up.error)throw up.error;const p:any={conversation_id:selectedId,content:'',message_type:'audio',media_path:path,media_mime:r.mimeType,media_name:'Voice message.webm',media_size:blob.size,media_duration_ms:durationMs};if(replyId)p.reply_to_message_id=replyId;const {error:e}=await supabase.from('messages').insert(p);if(e)throw e;setReplyId(null);await load()}catch(e){setError(e instanceof Error?e.message:'Could not save voice message.')}finally{setUploading(false);stream.getTracks().forEach(t=>t.stop());mediaStream.current=null;setRecording(false);setRecordingMs(0)}};r.start();mediaRecorder.current=r;setRecording(true);timer.current=window.setInterval(()=>{const ms=Math.min(Date.now()-recordingStarted.current,120000);setRecordingMs(ms);if(ms>=120000)r.stop()},200)}catch(e){setError(e instanceof Error?e.message:'Microphone permission was denied.')}};
src/app/pages/InboxPage.tsx:87:  <aside style={{display:listVisible?'block':'none',overflowY:'auto',padding:10,borderRight:mobile?0:'1px solid rgba(0,0,0,.1)'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><h1>Inbox</h1><button type="button">＋ Group</button></div><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search people..." style={{width:'100%',boxSizing:'border-box',marginTop:9,padding:9}}/>{search.trim()&&<div>{searchPeople.map(p=><button key={p.id} type="button" onClick={()=>void createDirect(p)} style={{display:'flex',width:'100%',gap:8,alignItems:'center',border:0,background:'transparent',padding:7,textAlign:'left'}}>{p.avatar_url?<img src={p.avatar_url} alt="" width={34} height={34} style={{borderRadius:'50%',objectFit:'cover'}}/>:<span>👤</span>}<span>{nameOf(p)}<small style={{display:'block'}}>@{p.username??''}</small></span></button>)}</div>}{loading&&<p>Loading chats…</p>}{error&&<p role="alert" style={{overflowWrap:'anywhere'}}>{error}</p>}{!loading&&!conversations.length&&<p>No conversations yet. Search for someone to start a chat</p>}{conversations.map(c=>{const member=members.find(m=>m.conversation_id===c.id&&m.profile_id!==profileId),title=c.kind==='group'?c.title??'Group':nameOf(member?.profile),avatar=c.kind==='group'?c.avatar_url:member?.profile?.avatar_url,last=messages.filter(m=>m.conversation_id===c.id&&!m.deleted_at).at(-1);return <button key={c.id} type="button" onClick={()=>void open(c.id)} style={{width:'100%',display:'flex',alignItems:'center',gap:9,padding:8,marginTop:5,border:'1px solid rgba(0,0,0,.07)',borderRadius:9,background:selectedId===c.id?'rgba(0,0,0,.06)':'#fff',textAlign:'left'}}>{avatar?<img src={avatar} alt="" width={36} height={36} style={{borderRadius:'50%',objectFit:'cover'}}/>:<span>👤</span>}<span style={{flex:1,minWidth:0,overflow:'hidden'}}><strong style={{display:'block',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{title}</strong><small style={{display:'block',overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}>{last?.message_type==='image'?'📷 Photo':last?.message_type==='video'?'🎥 Video':last?.message_type==='audio'?'🎤 Voice message':last?.content||'No messages yet'}</small></span></button>})}</aside>
src/app/pages/InboxPage.tsx:91:   <div ref={composerRef} style={{padding:9,borderTop:'1px solid rgba(0,0,0,.1)',display:'flex',gap:6,alignItems:'center',background:'#fff',overflow:'hidden'}}><input ref={fileInput} type="file" accept="image/*,video/*" multiple hidden onChange={e=>void sendFiles(e.target.files)}/><button type="button" onClick={()=>fileInput.current?.click()} disabled={uploading||recording} title="Send photo or video">＋</button><input value={draft} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();void sendText()}}} placeholder={editingId?'Edit message…':'Write a message...'} style={{flex:1,minWidth:0,padding:'9px 12px'}} disabled={recording}/>{recording?<button type="button" onClick={stopRecording}>⏹ {recordingLabel}</button>:<button type="button" onClick={()=>void startRecording()} disabled={!selectedId||!!draft.trim()||uploading}>🎤</button>}<button type="button" onClick={()=>void sendText()} disabled={!draft.trim()||uploading} >{editingId?'Save':'Send'}</button></div>
src/app/pages/NotificationsPage.tsx:13: const [items,setItems]=useState<NotificationRow[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null),[responding,setResponding]=useState<string|null>(null),[flash,setFlash]=useState<NotificationRow|null>(null);
src/app/pages/NotificationsPage.tsx:26: return <main style={{minWidth:0,position:'relative'}}><style>{`.team-invite-flash{position:fixed;right:14px;top:76px;z-index:1500;width:min(360px,calc(100% - 28px));padding:14px;border:1px solid rgba(255,255,255,.94);border-radius:18px;background:linear-gradient(145deg,rgba(236,253,245,.99),rgba(255,255,255,.99) 55%,rgba(239,246,255,.98));box-shadow:0 24px 65px rgba(15,23,42,.26),inset 0 1px 0 #fff;animation:teamFlash .22s ease-out}.team-invite-flash-title{font-size:11px;font-weight:950;color:#047857}.team-invite-flash-copy{margin-top:5px;font-size:10px;line-height:1.45;color:#334155}.team-invite-flash-actions{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:10px}.team-invite-btn{min-height:34px;border-radius:10px;border:1px solid rgba(100,116,139,.16);font-size:9px;font-weight:950;cursor:pointer;box-shadow:0 4px 0 rgba(15,23,42,.07),inset 0 1px 0 #fff}.team-invite-accept{background:linear-gradient(145deg,#10b981,#047857);color:#fff}.team-invite-decline{background:linear-gradient(145deg,#fff,#fef2f2);color:#b91c1c}.report-flash{position:fixed;right:14px;top:76px;z-index:1500;width:min(390px,calc(100% - 28px));padding:15px;border:1px solid rgba(255,255,255,.96);border-radius:20px;background:linear-gradient(145deg,rgba(239,246,255,.99),rgba(255,255,255,.99) 50%,rgba(236,253,245,.98));box-shadow:0 28px 75px rgba(15,23,42,.28),inset 0 1px 0 #fff;animation:teamFlash .22s ease-out}.report-flash-title{font-size:11px;font-weight:950;color:#4338ca}.report-flash-copy{margin-top:5px;color:#334155;font-size:10px;line-height:1.45}.report-flash-note{margin-top:9px;padding:9px;border-radius:11px;background:rgba(255,255,255,.72);color:#475569;font-size:9px;line-height:1.5}.report-flash-actions{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:10px}.report-btn{min-height:35px;border-radius:10px;border:1px solid rgba(100,116,139,.16);font-size:9px;font-weight:950;cursor:pointer}.report-view{background:linear-gradient(145deg,#4f46e5,#2563eb);color:#fff}.report-decline{background:linear-gradient(145deg,#fff,#fef2f2);color:#b91c1c}@keyframes teamFlash{from{opacity:0;transform:translateY(-8px) scale(.98)}to{opacity:1;transform:none}}`}</style>{flash&&flash.type==='team_invitation'&&<div className="team-invite-flash" role="alert"><div className="team-invite-flash-title">♛ Work Team Invitation</div><div className="team-invite-flash-copy">{invitationText(flash)}</div><div className="team-invite-flash-actions"><button className="team-invite-btn team-invite-accept" disabled={responding===flash.id} onClick={()=>void respond(flash,true)}>Accept</button><button className="team-invite-btn team-invite-decline" disabled={responding===flash.id} onClick={()=>void respond(flash,false)}>Decline</button></div></div>}{flash&&flash.type==='contractor_work_entry_report'&&<div className="report-flash" role="alert"><div className="report-flash-title">⚠️ Contractor Reported 1 Entry</div><div className="report-flash-copy"><strong>{reportText(flash).sender}</strong> reported one of your Team Work entries.</div><div className="report-flash-note">{reportText(flash).note}</div><div className="report-flash-actions"><button className="report-btn report-view" disabled={responding===flash.id} onClick={()=>void viewReport(flash)}>View Entry</button><button className="report-btn report-decline" disabled={responding===flash.id} onClick={()=>void declineReport(flash)}>Decline Report</button></div></div>}<div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:12,marginBottom:14,padding:'10px 2px 12px',borderBottom:'1px solid rgba(92,92,180,.10)'}}><div><h1 style={{margin:0,fontSize:'clamp(25px,7vw,34px)',fontWeight:850,background:'linear-gradient(135deg,#17152d,#4f46e5,#2563eb)',WebkitBackgroundClip:'text',backgroundClip:'text',color:'transparent'}}>Notifications</h1><div style={{display:'inline-flex',marginTop:7,padding:'4px 9px',borderRadius:999,background:unread?'rgba(79,70,229,.09)':'rgba(34,197,94,.09)',color:unread?'#4f46e5':'#16803c',fontSize:12,fontWeight:750}}>{unread?`${unread} unread`:'All caught up'}</div></div><button type="button" onClick={()=>void markAll()} disabled={!unread} style={{border:0,borderRadius:999,padding:'8px 13px',fontWeight:700,background:unread?'linear-gradient(135deg,#6d5dfc,#3b82f6)':'rgba(0,0,0,.07)',color:unread?'#fff':'rgba(0,0,0,.45)'}}>Mark all read</button></div>{error&&<p role="alert">{error}</p>}{loading&&<p>Loading notifications…</p>}{!loading&&!error&&!items.length&&<div style={{padding:22,textAlign:'center',border:'1px solid rgba(0,0,0,.08)',borderRadius:18,background:'linear-gradient(145deg,rgba(255,255,255,.96),rgba(245,247,255,.9))',boxShadow:'0 10px 28px rgba(0,0,0,.06)'}}>🔔 <strong>No notifications yet.</strong><p style={{margin:'6px 0 0',opacity:.62}}>Activity from your community will appear here.</p></div>}{!loading&&items.map(item=>{const icon=typeIcons[item.type]??'🔔';const attendance=item.type==='attendance_reminder';const invitation=item.type==='team_invitation';const report=item.type==='contractor_work_entry_report';const accepted=invitation&&item.metadata?.action_state==='accepted';const reportState=typeof item.metadata?.action_state==='string'?String(item.metadata.action_state):'pending';const reportData=report?reportText(item):null;return <article key={item.id} onClick={()=>void click(item)} style={{display:'flex',gap:11,alignItems:'flex-start',minWidth:0,padding:11,marginTop:9,border:item.is_read?'1px solid rgba(0,0,0,.08)':'1px solid rgba(91,91,245,.22)',borderRadius:15,background:item.is_read?'rgba(255,255,255,.88)':'linear-gradient(135deg,rgba(245,247,255,.98),rgba(255,255,255,.98))',boxShadow:item.is_read?'0 5px 16px rgba(0,0,0,.045)':'0 7px 20px rgba(80,82,190,.10)'}}><div style={{position:'relative',flexShrink:0}}>{attendance?<div style={{width:43,height:43,borderRadius:'50%',display:'grid',placeItems:'center',background:'linear-gradient(135deg,#fff7ed,#fef3c7)',fontSize:20}}>⏰</div>:item.sender?.avatar_url?<img src={item.sender.avatar_url} alt="" width={43} height={43} style={{borderRadius:'50%',objectFit:'cover',border:'2px solid #fff'}}/>:<div style={{width:43,height:43,borderRadius:'50%',display:'grid',placeItems:'center',background:'linear-gradient(135deg,#e9e7ff,#dbeafe)',fontSize:19}}>{report?'⚠️':'👤'}</div>}<span style={{position:'absolute',right:-3,bottom:-2,width:21,height:21,borderRadius:'50%',display:'grid',placeItems:'center',background:'#fff',boxShadow:'0 2px 7px rgba(0,0,0,.13)',fontSize:11}}>{icon}</span></div><div style={{flex:1,minWidth:0,lineHeight:1.35}}>{invitation?<><div><strong>{item.sender?.display_name??item.sender?.username??'A contractor'}</strong> invited you to join {pronoun(item.sender?.gender)} work team.</div><small style={{display:'block',marginTop:3,opacity:.55}}>{formatTime(item.created_at)}</small>{accepted?<div style={{marginTop:5,color:'#15803d',fontSize:9,fontWeight:900}}>🟢 Member</div>:<div style={{display:'flex',gap:6,marginTop:7}}><button type="button" disabled={responding===item.id} onClick={e=>{e.stopPropagation();void respond(item,true)}} style={{minHeight:30,padding:'0 10px',borderRadius:9,border:0,background:'linear-gradient(145deg,#10b981,#047857)',color:'#fff',fontSize:9,fontWeight:950}}>Accept</button><button type="button" disabled={responding===item.id} onClick={e=>{e.stopPropagation();void respond(item,false)}} style={{minHeight:30,padding:'0 10px',border:'1px solid rgba(185,28,28,.15)',borderRadius:9,background:'#fff',color:'#b91c1c',fontSize:9,fontWeight:950}}>Decline</button></div>}</>:report?<><div><strong>{reportData?.sender}</strong> reported one of your Team Work entries.</div><div style={{marginTop:7,padding:'8px 9px',borderRadius:10,background:'linear-gradient(145deg,#eff6ff,#f8fafc)',color:'#475569',fontSize:9,lineHeight:1.5}}>{reportData?.note}</div><small style={{display:'block',marginTop:5,opacity:.55}}>{formatTime(item.created_at)}</small>{reportState==='pending'?<div style={{display:'flex',gap:6,marginTop:8,flexWrap:'wrap'}}><button type="button" disabled={responding===item.id} onClick={e=>{e.stopPropagation();void viewReport(item)}} style={{minHeight:31,padding:'0 11px',borderRadius:9,border:0,background:'linear-gradient(145deg,#4f46e5,#2563eb)',color:'#fff',fontSize:9,fontWeight:950}}>View Entry</button><button type="button" disabled={responding===item.id} onClick={e=>{e.stopPropagation();void declineReport(item)}} style={{minHeight:31,padding:'0 11px',border:'1px solid rgba(185,28,28,.15)',borderRadius:9,background:'#fff',color:'#b91c1c',fontSize:9,fontWeight:950}}>Decline Report</button></div>:<div style={{marginTop:6,color:'#64748b',fontSize:9,fontWeight:850}}>Report ended.</div>}</>:attendance?<div><strong>Attendance Reminder</strong> — Please mark today’s attendance.</div>:<div><strong>{item.sender?.display_name??item.sender?.username??'Someone'}</strong> {labels[item.type]??'sent you a notification'}</div>}{!invitation&&!report&&<small style={{display:'block',marginTop:3,opacity:.55}}>{formatTime(item.created_at)}</small>}</div>{!item.is_read&&!invitation&&!report&&<span aria-label="Unread" style={{width:8,height:8,flexShrink:0,borderRadius:'50%',background:'#4f46e5',boxShadow:'0 0 0 4px rgba(79,70,229,.10)'}}/>}{report&&reportState==='pending'&&!item.is_read&&<span aria-label="Unread report" style={{width:8,height:8,flexShrink:0,borderRadius:'50%',background:'#4f46e5',boxShadow:'0 0 0 4px rgba(79,70,229,.10)'}}/>}</article>})}</main>
src/app/worker-diary-responsive.css:209:/* Timeline entries and empty/loading surfaces */
src/features/ai/components/MovableAiLauncher.tsx:50:        .ws-ai-loading{color:#86a995!important}.ws-ai-dot{color:#86efac!important}
src/features/ai/components/WorkSocialAiAssistant.tsx:51:  const [loadingHistory, setLoadingHistory] = useState(false);
src/features/ai/components/WorkSocialAiAssistant.tsx:261:        .ws-ai-loading{align-self:flex-start;color:#94a3b8;font-size:12px;padding:5px 2px}.ws-ai-dot{display:inline-block;animation:none}.ws-ai-dot:nth-child(2){animation-delay:.15s}.ws-ai-dot:nth-child(3){animation-delay:.3s}@keyframes ws-ai-pulse{50%{opacity:.25}}@keyframes ws-ai-mic-pulse{50%{transform:scale(.94);opacity:.7}}
src/features/ai/components/WorkSocialAiAssistant.tsx:266:        <aside className="ws-ai-history"><div className="ws-ai-history-title">Conversations</div><button className="ws-ai-new" type="button" onClick={newConversation}>＋ New conversation</button>{loadingHistory ? <div style={{ fontSize: 11, color: '#64748b', padding: 8 }}>Loading history…</div> : null}{conversations.map((item) => <button key={item.id} className={`ws-ai-conversation${item.id === conversationId ? ' active' : ''}`} type="button" onClick={() => void selectConversation(item.id)}><span className="ws-ai-conversation-title">{item.title || 'Untitled conversation'}</span><span className="ws-ai-conversation-date">{formatDate(item.updated_at)}</span></button>)}</aside>
src/features/ai/components/WorkSocialAiAssistant.tsx:272:            {sending ? <div className="ws-ai-loading">Work Social AI is thinking <span className="ws-ai-dot">·</span><span className="ws-ai-dot">·</span><span className="ws-ai-dot">·</span></div> : null}<div ref={endRef} />
src/features/ai/components/WorkSocialAiAssistantWithMode.tsx:45:  const offlineStatusCard = mode === 'offline' ? <section className="ws-ai-offline-status" aria-label="Offline AI status"><div className="ws-ai-offline-status-heading"><span className="ws-ai-offline-status-icon" aria-hidden="true">✦</span><div><div className="ws-ai-offline-status-title">Offline AI</div><div className="ws-ai-offline-status-state">{preparing ? 'Preparing Offline AI…' : localStatus.state === 'ready' ? 'Ready' : needsPreparation ? 'Needs preparation' : preparationFailed ? 'Preparation failed' : 'Needs preparation'}</div></div></div><p>{downloadComplete ? 'Download complete. Verifying model…' : hasDownloadProgress ? 'Downloading model…' : preparing ? 'This may take a little while.' : localStatus.state === 'ready' ? 'Your assistant is now available offline.' : needsPreparation ? 'Offline AI needs to be prepared on this device.' : 'Offline AI couldn’t be prepared automatically on this browser.'}</p>{hasDownloadProgress ? <div className="ws-ai-download-progress" aria-label="Offline AI model download progress"><div className="ws-ai-download-progress-meta"><span>{formatDownloadBytes(downloadProgress.receivedBytes)}{downloadProgress.totalBytes !== null ? ` / ${formatDownloadBytes(downloadProgress.totalBytes)}` : ' downloaded'}</span>{downloadPercent !== null ? <strong>{Math.round(downloadPercent)}%</strong> : <strong>Progress calculating…</strong>}</div>{downloadPercent !== null ? <div className="ws-ai-download-progress-track" role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={Math.round(downloadPercent)} aria-label={`Downloaded ${Math.round(downloadPercent)} percent`}><div className="ws-ai-download-progress-fill" style={{ width: `${downloadPercent}%` }} /></div> : <div className="ws-ai-download-progress-track" aria-hidden="true"><div className="ws-ai-download-progress-fill ws-ai-download-progress-indeterminate" /></div>}</div> : null}{preparationFailed ? <p className="ws-ai-offline-status-recovery">You can try again or import an existing GGUF model.</p> : null}{localStatus.state !== 'ready' && !preparing ? <div className="ws-ai-offline-status-actions"><button type="button" onClick={() => setPrepareAttempt((current) => current + 1)}>{needsPreparation ? 'Prepare Offline AI' : 'Try Again'}</button></div> : null}</section> : null;
src/features/ai/phase12Performance.test.ts:42:  equal(adapter.loadCalls, 1, 'concurrent loading of the same model performs one adapter load');
src/features/ai/premiumUxState.ts:12:  const modelLabel = input.reasonCode === 'MODEL_NOT_INSTALLED' ? 'Local Model · Not installed' : input.reasonCode === 'MODEL_INVALID' ? 'Local Model · Invalid' : input.reasonCode === 'MODEL_DOWNLOADING' ? 'Local Model · Downloading' : input.reasonCode === 'MODEL_VERIFYING' ? 'Local Model · Verifying' : input.reasonCode === 'MODEL_LOADING' ? 'Local Model · Loading' : localUnavailable ? 'Local Model · Not ready' : 'Local Model · Ready';
src/features/ai/providers/localAiProvider.ts:34:    if (webLocalAi.isPreparing()) return { state: 'unavailable', provider: this.id, mode: this.mode, reason: 'Offline AI is downloading and preparing the local model.', reasonCode: 'MODEL_DOWNLOADING' };
src/features/ai/providers/localAiProvider.ts:53:    if (webLocalAi.isPreparing()) return { state: 'unavailable', provider: this.id, mode: this.mode, reason: 'Offline AI is downloading and preparing the local model.', reasonCode: 'MODEL_DOWNLOADING' };
src/features/ai/providers/localAiProvider.ts:57:    if (runtimeState !== 'MODEL_READY') return { state: 'unavailable', provider: this.id, mode: this.mode, reason: runtimeState === 'UNINITIALIZED' ? 'Local runtime is not initialized.' : runtimeState === 'INITIALIZING' ? 'Local runtime is initializing.' : runtimeState === 'LOADING_MODEL' ? 'Verified local model is loading.' : 'Local runtime is busy and cannot accept a new route.', reasonCode: runtimeState === 'INITIALIZING' ? 'RUNTIME_INITIALIZING' : runtimeState === 'LOADING_MODEL' ? 'MODEL_LOADING' : 'LOCAL_RUNTIME_UNAVAILABLE' };
src/features/ai/runtime/localInferenceRuntime.ts:18:  private loadingModelKey: string | null = null;
src/features/ai/runtime/localInferenceRuntime.ts:69:      if (this.loadingModelKey === modelKey) return this.loadModelPromise;
src/features/ai/runtime/localInferenceRuntime.ts:70:      throw new LocalInferenceRuntimeError('INVALID_STATE', 'A different model is already loading.');
src/features/ai/runtime/localInferenceRuntime.ts:74:    this.loadingModelKey = modelKey;
src/features/ai/runtime/localInferenceRuntime.ts:90:        throw this.runtimeError(error, 'Verified model loading failed.', 'MODEL_LOAD_FAILED');
src/features/ai/runtime/localInferenceRuntime.ts:96:      if (this.loadingModelKey === modelKey) this.loadingModelKey = null;
src/features/ai/runtime/localInferenceRuntime.ts:300:      this.loadingModelKey = null;
src/features/auth/components/LoginForm.tsx:9:  const [loading, setLoading] = useState(false);
src/features/auth/components/LoginForm.tsx:266:    <button onClick={handleGoogle} disabled={loading}>Continue with Google</button>
src/features/auth/components/LoginForm.tsx:271:      <button type="submit" disabled={loading}>{loading ? 'Please wait…' : 'Sign In'}</button>
src/features/auth/components/SignupForm.tsx:10:  const [loading, setLoading] = useState(false);
src/features/auth/components/SignupForm.tsx:74:    <button type="button" onClick={handleGoogle} disabled={loading}>Continue with Google</button>
src/features/auth/components/SignupForm.tsx:80:      <button type="submit" disabled={loading}>{loading ? 'Creating…' : 'Create account'}</button>
src/features/contractor/components/ContractorAddMembersWorkflow.tsx:9: const [open,setOpen]=useState(false),[membersOpen,setMembersOpen]=useState(false),[query,setQuery]=useState(''),[results,setResults]=useState<Worker[]>([]),[members,setMembers]=useState<Member[]>([]),[loading,setLoading]=useState(false),[membersLoading,setMembersLoading]=useState(false),[error,setError]=useState(''),[sending,setSending]=useState<string|null>(null),[removing,setRemoving]=useState<string|null>(null),[removeTarget,setRemoveTarget]=useState<Member|null>(null),[notice,setNotice]=useState('');
src/features/contractor/components/ContractorAddMembersWorkflow.tsx:17: return <>{notice&&<div className="ctm-toast" role="status">✓ Team updated<div>{notice}</div></div>}<style>{css}</style>{open&&<div className="cam-backdrop" onMouseDown={e=>{if(e.currentTarget===e.target)setOpen(false)}}><section className="cam-panel" role="dialog" aria-modal="true"><div className="cam-head"><div><div className="cam-kicker">TEAM MEMBERS · {teamNumber}</div><h2 className="cam-title">Add Members</h2><p className="cam-sub">Search real Work Social workers before sending them into this team.</p></div><button className="cam-close" type="button" onClick={()=>setOpen(false)}>×</button></div><label className="cam-search"><span>⌕</span><input autoFocus className="cam-input" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search worker name or Worker ID…"/></label><div className="cam-hint">Supports Worker name, username, or unique Worker ID number.</div>{query.trim().length<2?<div className="cam-status">Type at least 2 characters to search the real worker directory.</div>:loading?<div className="cam-status">Searching workers…</div>:error?<div className="cam-status cam-error">{error}</div>:results.length?<div className="cam-results">{results.map(w=><article className="cam-result" key={w.profile_id}><div className="cam-avatar">{w.avatar_url?<img src={w.avatar_url} alt=""/>:initials(w.display_name||'')}</div><div><div className="cam-result-name">{w.display_name||'Unnamed worker'}</div>{w.username&&<div className="cam-result-user">@{w.username}</div>}<div className="cam-result-id">Worker ID · {w.work_id}</div></div><span className="cam-type">{w.worker_type==='salary_person'?'Salary':'Contract'}</span><div className="cam-buttons"><button className="cam-btn cam-view" type="button" onClick={()=>view(w.profile_id)}>View</button><button className={`cam-btn ${w.member_status==='pending'?'cam-pending':'cam-add'}`} type="button" disabled={sending===w.profile_id||w.member_status!=='available'} onClick={()=>void addWorker(w)}>{sending===w.profile_id?'Sending…':w.member_status==='pending'?'Pending':w.member_status==='member'?'Member':'＋ Add'}</button></div></article>)}</div>:<div className="cam-status">No worker matched “{query.trim()}”. Try the worker name or Worker ID.</div>}<div className="cam-foot"><strong>Real worker directory.</strong> View opens only the public Work Social profile. Add creates a real pending team invitation.</div></section></div>}{membersOpen&&<div className="ctm-backdrop" onMouseDown={e=>{if(e.currentTarget===e.target)setMembersOpen(false)}}><section className="ctm-panel" role="dialog" aria-modal="true" aria-labelledby="team-members-title"><div className="ctm-head"><div><div className="ctm-kicker">TEAM MEMBERS · {teamNumber}</div><h2 className="ctm-title" id="team-members-title">My Team Members</h2></div><button className="ctm-close" type="button" onClick={()=>setMembersOpen(false)}>×</button></div><div className="ctm-intro"><h3 className="ctm-intro-title">Your Team. Your People.</h3><p className="ctm-intro-copy">Manage real workers in this team with a premium workspace view. Open a profile or safely remove a member or cancel a pending invitation.</p></div>{membersLoading?<div className="ctm-empty">Loading real team members…</div>:error?<div className="ctm-empty ctm-error">{error}</div>:members.length?<div className="ctm-list">{members.map(m=><article className="ctm-row" key={`${m.status}-${m.profile_id}`}><div className="ctm-avatar">{m.avatar_url?<img src={m.avatar_url} alt=""/>:initials(m.display_name||'')}</div><div className="ctm-row-main"><div className="ctm-row-top"><div><div className="ctm-name">{m.display_name||'Unnamed worker'}</div>{m.username&&<div className="ctm-user">@{m.username}</div>}</div><span className={`ctm-status ${m.status==='member'?'ctm-member':'ctm-pending'}`}>{m.status==='member'?'🟢 Member':'Pending'}</span></div></div><div className="ctm-actions"><button className="ctm-action ctm-view" type="button" onClick={()=>view(m.profile_id)}>View</button><button className="ctm-action ctm-remove" type="button" disabled={removing===m.profile_id} onClick={()=>setRemoveTarget(m)}>{removing===m.profile_id?'Removing…':'Remove'}</button></div></article>)}</div>:<div className="ctm-empty">No team members or pending invitations yet.</div>}</section></div>}{removeTarget&&<div className="ctm-confirm-backdrop" onMouseDown={e=>{if(e.currentTarget===e.target&&!removing)setRemoveTarget(null)}}><section className="ctm-confirm" role="alertdialog" aria-modal="true" aria-labelledby="remove-member-title"><h3 className="ctm-confirm-title" id="remove-member-title">Remove {removeTarget.display_name||'this worker'}?</h3><p className="ctm-confirm-copy">{removeTarget.status==='pending'?'This will cancel the pending team invitation.':'This will remove the worker from this team.'} This action only changes this team relationship.</p><div className="ctm-confirm-actions"><button className="ctm-confirm-btn ctm-cancel" type="button" disabled={!!removing} onClick={()=>setRemoveTarget(null)}>Keep</button><button className="ctm-confirm-btn ctm-danger" type="button" disabled={!!removing} onClick={()=>void confirmRemove()}>{removing?'Removing…':'Remove'}</button></div></section></div>}</>;
src/features/contractor/components/ContractorOverviewFinanceIntelligence.tsx:44:  const [loading, setLoading] = useState(true);
src/features/contractor/components/ContractorOverviewFinanceIntelligence.tsx:64:  if (loading) return <section className="cofi cofi-loading" aria-label="Loading Finance Intelligence"><style>{styles}</style><div className="cofi-loader"/><div><span className="cofi-eyebrow">FINANCE INTELLIGENCE</span><strong>Reading all financial layers…</strong></div></section>;
src/features/contractor/components/ContractorOverviewFinanceIntelligence.tsx:114:.cofi-core-stage{position:relative;min-height:158px;display:grid;place-items:center;perspective:650px}.cofi-shadow{position:absolute;bottom:4px;width:115px;height:28px;border-radius:50%;background:rgba(0,0,0,.5);filter:blur(10px);transform:rotateX(70deg)}.cofi-halo{position:absolute;border-radius:50%;border:1px solid rgba(103,232,249,.1)}.halo-a{width:153px;height:153px;box-shadow:0 0 34px rgba(34,211,238,.07);animation:cofi-breathe 6s ease-in-out infinite}.halo-b{width:132px;height:132px;border-style:dashed;border-color:rgba(167,139,250,.13);transform:rotateX(62deg) rotateZ(20deg);animation:cofi-drift 9s ease-in-out infinite}.cofi-ring{position:absolute;border-radius:50%;transform-style:preserve-3d}.ring-back{width:142px;height:142px;border:1px solid rgba(148,163,184,.15);box-shadow:0 8px 18px rgba(0,0,0,.25),inset 0 0 20px rgba(56,189,248,.03)}.ring-track{width:130px;height:130px;border:8px solid rgba(51,65,85,.68);box-shadow:inset 0 2px 3px rgba(255,255,255,.06)}.ring-progress{--coverage:0deg;width:130px;height:130px;border:8px solid transparent;border-top-color:#67e8f9;border-right-color:#818cf8;border-bottom-color:rgba(129,140,248,.12);transform:rotate(-90deg);mask:conic-gradient(from 0deg,#000 0 var(--coverage),transparent var(--coverage) 360deg);-webkit-mask:conic-gradient(from 0deg,#000 0 var(--coverage),transparent var(--coverage) 360deg);filter:drop-shadow(0 0 7px rgba(34,211,238,.3))}.cofi-chamber{position:relative;width:101px;height:101px;border-radius:50%;display:grid;place-items:center;transform:translateZ(22px);background:radial-gradient(circle at 30% 20%,rgba(255,255,255,.14),rgba(30,41,72,.9) 40%,#050b1a 80%);border:1px solid rgba(255,255,255,.13);box-shadow:inset 0 4px 9px rgba(255,255,255,.07),inset 0 -10px 20px rgba(0,0,0,.48),0 16px 25px rgba(0,0,0,.34),0 0 24px rgba(79,70,229,.14)}.cofi-glass{position:absolute;inset:12px;border-radius:50%;border:1px solid rgba(103,232,249,.1);background:radial-gradient(circle at 30% 20%,rgba(255,255,255,.08),transparent 35%)}.cofi-core-value{position:relative;z-index:2;display:grid;place-items:center;text-align:center}.cofi-core-value strong{font-size:21px;line-height:1;color:#f8fbff;font-weight:950;letter-spacing:-.05em;text-shadow:0 0 15px rgba(103,232,249,.18)}.cofi-core-value span{margin-top:4px;font-size:5px;letter-spacing:.16em;color:#67e8f9;font-weight:950}.cofi-marker{position:absolute;z-index:5;padding:3px 5px;border-radius:6px;background:rgba(2,6,23,.7);border:1px solid rgba(103,232,249,.14);font-size:5px;letter-spacing:.12em;color:#94a3b8}.marker-one{left:9px;top:20px}.marker-two{right:8px;bottom:20px}.cofi-data{min-width:0}.cofi-primary{padding:8px 9px;border-radius:13px;background:rgba(2,6,23,.35);border:1px solid rgba(148,163,184,.11);box-shadow:inset 0 1px 0 rgba(255,255,255,.04)}.cofi-primary span{display:block;font-size:5px;letter-spacing:.16em;color:#64748b;font-weight:950}.cofi-primary strong{display:block;margin-top:3px;font-size:9px;line-height:1.25;color:#e2e8f0;font-weight:850}.cofi-metrics{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px;margin-top:7px}.cofi-node{display:flex;align-items:center;gap:7px;min-width:0;padding:8px;border-radius:12px;background:linear-gradient(145deg,rgba(15,23,42,.7),rgba(30,41,59,.3));border:1px solid rgba(148,163,184,.1);box-shadow:inset 0 1px 0 rgba(255,255,255,.04)}.cofi-node b{display:grid;place-items:center;width:22px;height:22px;flex:none;border-radius:8px;background:rgba(34,211,238,.1);color:#67e8f9;font-size:9px}.cofi-node.risk b{background:rgba(251,113,133,.12);color:#ffe4e6}.cofi-node span{display:block;font-size:5px;letter-spacing:.12em;color:#64748b;font-weight:950}.cofi-node strong{display:block;margin-top:2px;font-size:8px;color:#f8fafc;font-weight:900}.cofi-secondary{display:flex;flex-wrap:wrap;justify-content:space-between;gap:7px;margin-top:7px;font-size:5px;letter-spacing:.1em;color:#64748b;font-weight:900}.cofi-secondary strong{color:#cbd5e1}.cofi-bottom{position:relative;z-index:2;display:flex;justify-content:space-between;align-items:center;gap:8px;margin-top:8px;padding:6px 2px 0}.cofi-source{display:flex;align-items:center;gap:5px;font-size:5px;letter-spacing:.12em;color:#64748b;font-weight:900}.cofi-source i{width:5px;height:5px;border-radius:50%;background:#22d3ee;box-shadow:0 0 8px rgba(34,211,238,.5)}.cofi-coverage-label{font-size:5px;letter-spacing:.1em;color:#67e8f9;font-weight:950}.cofi-loading,.cofi-error{display:flex;align-items:center;gap:10px}.cofi-loader{width:28px;height:28px;border-radius:50%;border:3px solid rgba(148,163,184,.16);border-top-color:#67e8f9;animation:cofi-spin 1s linear infinite}.cofi-error p{margin:4px 0;color:#94a3b8;font-size:8px}.cofi-error button{margin-left:auto;border:1px solid rgba(103,232,249,.2);background:rgba(34,211,238,.08);color:#a5f3fc;border-radius:9px;padding:6px 9px;font-size:7px;font-weight:900}@keyframes cofi-spin{to{transform:rotate(360deg)}}@keyframes cofi-breathe{50%{transform:scale(1.035);opacity:.75}}@keyframes cofi-drift{50%{transform:rotateX(62deg) rotateZ(200deg) scale(.96)}}@media(max-width:620px){.cofi{padding:12px}.cofi-command{grid-template-columns:1fr;gap:4px}.cofi-core-stage{min-height:145px}.cofi-head{display:block}.cofi-state{margin-top:7px}.cofi-head p{max-width:none}.cofi-bottom{align-items:flex-start;flex-direction:column}.cofi-coverage-label{align-self:flex-end}}@media(max-width:380px){.cofi-metrics{grid-template-columns:1fr}.cofi-secondary{display:grid;grid-template-columns:1fr 1fr}.cofi-secondary span:last-child{grid-column:1/-1}}
src/features/contractor/components/ContractorPersonalDashboard.tsx:33: const[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(''),[newOpen,setNewOpen]=useState(false),[trashOpen,setTrashOpen]=useState(false),[detail,setDetail]=useState<{type:PeriodCard;period:Period;entries:Entry[]}|null>(null),[viewEntry,setViewEntry]=useState<Entry|null>(null),[editEntry,setEditEntry]=useState<Entry|null>(null),[deleteEntry,setDeleteEntry]=useState<Entry|null>(null),[menuId,setMenuId]=useState<string|null>(null),[moreLoading,setMoreLoading]=useState(false),[trashLoading,setTrashLoading]=useState(false),[detailLoading,setDetailLoading]=useState(false);
src/features/contractor/components/ContractorPersonalDashboard.tsx:51: <section className="cd-section"><h2 style={{margin:0,fontSize:15}}>Recent Entries</h2><div style={{margin:'3px 0 9px',fontSize:10,color:'#64748b'}}>{loading?'Loading…':`${entries.length} recent entr${entries.length===1?'y':'ies'}${entriesHasMore?' · more available':''}`}</div>{!loading&&!entries.length?<div className="cd-empty">No entries yet.</div>:entries.map(e=><EntryRow key={e.id} entry={e}/>)}{entriesHasMore&&<button className="cd-more-list" type="button" disabled={moreLoading} onClick={()=>void loadMore()}>{moreLoading?'Loading older entries…':'More'}</button>}</section>
src/features/contractor/components/ContractorTeamListMenu.tsx:13:  const [loading, setLoading] = useState(false);
src/features/contractor/components/ContractorTeamListMenu.tsx:118:      {loading ? <div className="ctm-state"><span className="ctm-spinner" /> Loading your teams…</div> : error ? <div className="ctm-error">Unable to load teams<br /><small>{error}</small><button type="button" onClick={() => void loadTeams()}>Retry</button></div> : teams.length === 0 ? <div className="ctm-empty"><div className="ctm-empty-icon">＋</div><strong>No teams yet</strong><span>Create your first team from the Create a Team action.</span></div> : <div className="ctm-list">{teams.map((team, index) => <button key={team.team_number} type="button" className="ctm-team" onClick={() => { setOpen(false); navigate(`/work/contractor?view=team&team=${encodeURIComponent(String(team.team_number))}`); }}><span className={`ctm-avatar ctm-avatar-${index % 4}`}>{team.name.trim().charAt(0).toUpperCase() || 'T'}</span><span className="ctm-copy"><strong>{team.name}</strong><span>{team.purpose}</span><em>TEAM ID · {team.team_number}</em></span><span className="ctm-arrow">›</span></button>)}</div>}
src/features/contractor/components/ContractorTeamTrash.tsx:14: const [entries,setEntries]=useState<TrashEntry[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[busy,setBusy]=useState<string|null>(null),[confirmId,setConfirmId]=useState<string|null>(null),[menuId,setMenuId]=useState<string|null>(null),[detailsId,setDetailsId]=useState<string|null>(null),[menuPosition,setMenuPosition]=useState<{top:number;right:number}|null>(null);
src/features/contractor/components/ContractorTeamTrash.tsx:22: return <div className="ctd-panel-backdrop" role="presentation" onMouseDown={e=>{if(e.currentTarget===e.target){setMenuId(null);setMenuPosition(null);onClose()}}}><section className="ctd-panel ctt-panel" role="dialog" aria-modal="true" aria-label="Team Work Trash"><div className="ctd-panel-head"><div><div className="ctd-panel-title">🗑️ TEAM WORK TRASH</div><div className="ctt-sub">Owner control · Worker deleted entries remain recoverable</div><div className="ctt-team-banner"><span>TEAM</span><b>{teamName||`Team ${teamNumber}`}</b><em>#{teamNumber}</em></div></div><button type="button" className="ctd-close" onClick={onClose}>×</button></div>{loading?<div className="ctd-member-empty">Loading real deleted Team Work…</div>:error?<div className="ctd-member-empty ctt-error">{error}</div>:!entries.length?<div className="ctd-member-empty">Trash is empty.</div>:<div className="ctt-list">{entries.map(e=><article className="ctt-card" key={e.id}><div className="ctt-row"><div className="ctt-main"><div className="ctt-name">{e.item_name}</div><div className="ctt-worker"><span className="ctt-avatar" aria-hidden="true">{e.worker_avatar_url?<img src={e.worker_avatar_url} alt=""/>:initials(workerName(e))}</span><span className="ctt-worker-name">{workerName(e)}</span>{e.worker_username&&e.worker_display_name&&<span className="ctt-username">@{e.worker_username}</span>}</div></div><div className="ctt-head-actions"><div className="ctt-amount">{money(e.total)}</div><div className="ctt-menu-wrap"><button type="button" className="ctt-menu-btn" aria-label={`More actions for ${e.item_name}`} aria-haspopup="menu" aria-expanded={menuId===e.id} onClick={event=>openMenu(e.id,event.currentTarget)}>⋮</button></div></div></div><div className="ctt-meta">Team · {teamName||`Team ${teamNumber}`} · #{teamNumber}</div><div className="ctt-meta">Deleted from active work · Qty {e.quantity} · Rate {money(e.rate)} · {when(e.updated_at)}</div>{e.size?.length&&<div className="ctt-size">Size · {e.size.join(' × ')}</div>}{e.special_note&&<div className="ctt-note">{e.special_note}</div>}</article>)}</div>}{menu}{confirmId&&createPortal(<div className="ctt-confirm-layer" role="presentation"><div className="ctt-confirm"><div className="ctt-confirm-icon">⚠️</div><strong>Remove this Team Work entry permanently?</strong><p>This cannot be undone. The canonical worker Team Work entry will be removed; immutable audit history is retained.</p><div><button type="button" onClick={()=>setConfirmId(null)} className="ctt-cancel">Cancel</button><button type="button" onClick={()=>void purge(confirmId)} className="ctt-delete">Remove Permanent</button></div></div></div>,document.body)}{details&&<div className="ctt-details-backdrop" role="presentation" onMouseDown={e=>{if(e.currentTarget===e.target)setDetailsId(null)}}><section className="ctt-details" role="dialog" aria-modal="true" aria-labelledby="ctt-details-title"><div className="ctt-details-head"><div><div className="ctt-details-kicker">TRASHED TEAM WORK</div><h2 id="ctt-details-title">{details.item_name}</h2><div className="ctt-details-worker"><span className="ctt-details-avatar" aria-hidden="true">{details.worker_avatar_url?<img src={details.worker_avatar_url} alt=""/>:initials(workerName(details))}</span><div><p>Worker</p><b>{workerName(details)}</b>{details.worker_username&&details.worker_display_name&&<small>@{details.worker_username}</small>}</div></div><div className="ctt-details-team"><span>Team</span><b>{teamName||`Team ${teamNumber}`}</b><em>#{teamNumber}</em></div></div><button type="button" className="ctd-close" onClick={()=>setDetailsId(null)}>×</button></div><div className="ctt-details-grid"><div><span>Quantity</span><b>{details.quantity} pcs</b></div><div><span>Rate</span><b>{money(details.rate)}</b></div><div><span>Total</span><b>{money(details.total)}</b></div><div><span>Deleted</span><b>{when(details.updated_at)}</b></div><div><span>Occurred</span><b>{when(details.occurred_at)}</b></div><div><span>Size</span><b>{details.size?.length?details.size.join(' × '):'No size'}</b></div></div>{details.special_note&&<div className="ctt-details-note"><span>Special Note</span><p>{details.special_note}</p></div>}<div className="ctt-details-actions"><button type="button" className="ctt-restore" disabled={busy===details.id} onClick={()=>void restore(details.id)}>↶ Restore Back</button><button type="button" className="ctt-delete" disabled={busy===details.id} onClick={()=>{setDetailsId(null);setConfirmId(details.id)}}>Remove Permanent</button></div></section></div>}</section><style>{`.ctt-sub{margin-top:3px;color:#94a3b8;font-size:8px;font-weight:750}.ctt-team-banner{display:flex;align-items:center;gap:7px;margin-top:8px;padding:8px 9px;border-radius:11px;background:linear-gradient(145deg,#ecfdf5,#eff6ff);border:1px solid rgba(16,185,129,.14)}.ctt-team-banner span{font-size:7px;letter-spacing:.12em;font-weight:950;color:#64748b}.ctt-team-banner b{font-size:9px;font-weight:950;color:#172033;overflow-wrap:anywhere}.ctt-team-banner em,.ctt-details-team em{font-style:normal;margin-left:auto;font-size:7px;font-weight:900;color:#047857}.ctt-list{display:grid;gap:9px;max-height:62vh;overflow:auto;padding:2px}.ctt-card{position:relative;padding:13px;border-radius:17px;border:1px solid rgba(226,232,240,.9);background:linear-gradient(145deg,#fff,#fff7f7);box-shadow:0 9px 20px rgba(15,23,42,.06),inset 0 1px 0 #fff}.ctt-row{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.ctt-main{min-width:0}.ctt-name{font-size:12px;font-weight:950;overflow-wrap:anywhere}.ctt-worker{display:flex;align-items:center;gap:6px;margin-top:5px;min-width:0;color:#64748b;font-size:8px;font-weight:850}.ctt-avatar{width:27px;height:27px;flex:0 0 27px;border-radius:9px;display:grid;place-items:center;overflow:hidden;background:linear-gradient(145deg,#dbeafe,#d1fae5);border:1px solid rgba(15,118,110,.13);color:#0f766e;font-size:9px;font-weight:950;box-shadow:0 4px 9px rgba(15,23,42,.08),inset 0 1px 0 #fff}.ctt-avatar img{width:100%;height:100%;object-fit:cover}.ctt-worker-name{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#0f766e}.ctt-username{color:#94a3b8;white-space:nowrap}.ctt-head-actions{display:flex;align-items:center;gap:6px;flex:0 0 auto}.ctt-amount{font-size:13px;font-weight:950;color:#b91c1c;white-space:nowrap}.ctt-menu-wrap{position:relative}.ctt-menu-btn{width:34px;height:34px;border:1px solid rgba(226,232,240,.95);border-radius:11px;background:linear-gradient(145deg,#fff,#f8fafc);box-shadow:0 5px 12px rgba(15,23,42,.08),inset 0 1px 0 #fff;color:#334155;font-size:19px;line-height:1;font-weight:950;cursor:pointer}.ctt-menu-btn:active{transform:translateY(1px)}.ctt-menu{z-index:10000;width:205px;padding:6px;border:1px solid rgba(255,255,255,.95);border-radius:15px;background:rgba(255,255,255,.98);box-shadow:0 18px 42px rgba(15,23,42,.18),inset 0 1px 0 #fff;backdrop-filter:blur(12px)}.ctt-menu button{width:100%;display:flex;align-items:center;gap:9px;padding:9px;border:0;border-radius:10px;background:transparent;text-align:left;color:#172033;cursor:pointer}.ctt-menu button:hover{background:#f8fafc}.ctt-menu button>span:first-child{width:25px;height:25px;display:grid;place-items:center;border-radius:8px;background:#f1f5f9;color:#475569;font-size:12px;font-weight:950}.ctt-menu b{display:block;font-size:9px;font-weight:950}.ctt-menu small{display:block;margin-top:2px;color:#94a3b8;font-size:7px;font-weight:700}.ctt-menu-danger{color:#b91c1c!important}.ctt-menu-danger>span:first-child{background:#fef2f2!important;color:#dc2626!important}.ctt-meta{margin-top:8px;color:#64748b;font-size:8px;line-height:1.45}.ctt-size{margin-top:6px;color:#475569;font-size:8px;font-weight:800}.ctt-note{margin-top:7px;padding:7px 8px;border-radius:9px;background:#f8fafc;color:#475569;font-size:8px}.ctt-error{color:#b91c1c}.ctt-confirm-layer{position:fixed;z-index:20000;inset:0;display:grid;place-items:end center;padding:16px;box-sizing:border-box;pointer-events:none}.ctt-confirm{width:min(560px,100%);box-sizing:border-box;padding:14px;border-radius:17px;background:#fff;border:1px solid rgba(254,202,202,.95);box-shadow:0 24px 55px rgba(15,23,42,.22);pointer-events:auto}.ctt-confirm-icon{font-size:20px}.ctt-confirm strong{display:block;margin-top:5px;font-size:11px}.ctt-confirm p{margin:4px 0 10px;color:#64748b;font-size:8px;line-height:1.45}.ctt-confirm>div:last-child{display:flex;gap:6px}.ctt-actions button,.ctt-confirm button,.ctt-details-actions button{min-height:36px;padding:0 10px;border-radius:10px;font-size:8px;font-weight:950;cursor:pointer}.ctt-cancel{border:1px solid #e2e8f0;background:#fff;color:#475569}.ctt-restore{border:1px solid rgba(16,185,129,.25);background:#ecfdf5;color:#047857}.ctt-delete{border:0;background:#dc2626;color:#fff}.ctt-details-backdrop{position:absolute;z-index:25;inset:0;background:rgba(15,23,42,.25);backdrop-filter:blur(6px);display:grid;place-items:center;padding:15px}.ctt-details{width:min(360px,100%);max-height:80vh;overflow:auto;padding:15px;border:1px solid rgba(255,255,255,.95);border-radius:19px;background:linear-gradient(145deg,#fff,#f8fffd);box-shadow:0 25px 60px rgba(15,23,42,.22)}.ctt-details-head{display:flex;align-items:flex-start;justify-content:space-between;gap:9px}.ctt-details-kicker{color:#047857;font-size:7px;letter-spacing:.15em;font-weight:950}.ctt-details h2{margin:5px 0 0;font-size:18px;font-weight:950;overflow-wrap:anywhere}.ctt-details-worker{display:flex;align-items:center;gap:8px;margin-top:8px}.ctt-details-worker p{margin:0;color:#94a3b8;font-size:7px;font-weight:850;text-transform:uppercase;letter-spacing:.08em}.ctt-details-worker b{display:block;margin-top:2px;color:#172033;font-size:9px;font-weight:950}.ctt-details-worker small{display:block;margin-top:2px;color:#94a3b8;font-size:7px}.ctt-details-avatar{width:34px;height:34px;flex:0 0 34px;border-radius:11px;display:grid;place-items:center;overflow:hidden;background:linear-gradient(145deg,#dbeafe,#d1fae5);border:1px solid rgba(15,118,110,.13);color:#0f766e;font-size:10px;font-weight:950}.ctt-details-avatar img{width:100%;height:100%;object-fit:cover}.ctt-details-team{display:flex;align-items:center;gap:7px;margin-top:8px;padding:7px 8px;border-radius:10px;background:#ecfdf5;border:1px solid rgba(16,185,129,.14)}.ctt-details-team span{color:#64748b;font-size:7px;font-weight:850;text-transform:uppercase;letter-spacing:.08em}.ctt-details-team b{font-size:8px;font-weight:950}.ctt-details-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:13px}.ctt-details-grid>div{padding:9px;border-radius:11px;background:#f8fafc;border:1px solid #eef2f7}.ctt-details-grid span,.ctt-details-note span{display:block;color:#94a3b8;font-size:7px;font-weight:850;text-transform:uppercase;letter-spacing:.08em}.ctt-details-grid b{display:block;margin-top:3px;color:#172033;font-size:9px;font-weight:950;overflow-wrap:anywhere}.ctt-details-note{margin-top:8px;padding:9px;border-radius:11px;background:#f8fafc}.ctt-details-note p{margin:4px 0 0;color:#475569;font-size:8px;line-height:1.45}.ctt-details-actions{display:flex;gap:7px;margin-top:11px}.ctt-details-actions button{flex:1}@media(max-width:370px){.ctt-menu{width:190px}.ctt-details-grid{grid-template-columns:1fr}.ctt-details-actions{flex-direction:column}}`}</style></div>;
src/features/contractor/components/ContractorTeamWorkerIntelligence.tsx:30:  const [loading, setLoading] = useState(true);
src/features/contractor/components/ContractorTeamWorkerIntelligence.tsx:62:  if (loading) return <section className="ctwi" aria-labelledby="ctwi-title"><style>{styles}</style><div className="ctwi-state"><div className="ctwi-orb"/><div><div className="ctwi-kicker">TEAM INTELLIGENCE</div><h2 id="ctwi-title" className="ctwi-title">Reading team movement…</h2><p className="ctwi-copy">Loading real team and output records.</p></div></div></section>;
src/features/contractor/components/ContractorTrashButton.tsx:7:export function ContractorTrashButton({profileId}:{profileId:string}){const[open,setOpen]=useState(false),[rows,setRows]=useState<TrashEntry[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState('');const load=async()=>{setLoading(true);setError('');const r=await supabase.from('contractor_work_entry_trash').select(select).eq('profile_id',profileId).order('deleted_at',{ascending:false}).limit(100);if(r.error)setError(r.error.message);else setRows((r.data??[]) as TrashEntry[]);setLoading(false)};useEffect(()=>{if(open)void load()},[open,profileId]);return <><button type="button" aria-label="Open Contractor Trash" onClick={()=>setOpen(true)} style={{position:'fixed',right:16,top:92,zIndex:1100,width:46,height:46,borderRadius:16,border:'1px solid rgba(255,255,255,.72)',background:'linear-gradient(145deg,#fff,#fee2e2)',color:'#b91c1c',fontSize:21,boxShadow:'0 12px 26px rgba(15,23,42,.16)',cursor:'pointer'}}>🗑️</button>{open&&<div style={{position:'fixed',inset:0,zIndex:1500,display:'grid',placeItems:'center',padding:12,background:'rgba(15,23,42,.5)',backdropFilter:'blur(9px)'}} onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><section role="dialog" aria-modal="true" style={{width:'100%',maxWidth:620,maxHeight:'calc(100dvh - 24px)',overflow:'auto',padding:17,borderRadius:24,border:'1px solid rgba(255,255,255,.8)',background:'linear-gradient(145deg,#fff,#fff7f7)',boxShadow:'0 24px 60px rgba(15,23,42,.24)'}}><header style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start'}}><div><div style={{fontSize:9,fontWeight:950,letterSpacing:'.14em',color:'#b91c1c'}}>CONTRACTOR TRASH</div><h2 style={{margin:'4px 0'}}>Deleted Entries</h2><p style={{margin:0,color:'#64748b',fontSize:11}}>Deleted work records are kept here safely.</p></div><button type="button" onClick={()=>setOpen(false)}>×</button></header>{loading&&<p>Loading trash…</p>}{error&&<p role="alert" style={{color:'#b91c1c'}}>{error}</p>}{!loading&&!rows.length&&!error&&<div style={{marginTop:14,padding:18,textAlign:'center',borderRadius:16,background:'#f8fafc',color:'#64748b'}}>Trash is empty.</div>}<div style={{display:'grid',gap:8,marginTop:12}}>{rows.map(row=><article key={row.id} style={{padding:12,borderRadius:15,border:'1px solid rgba(239,68,68,.12)',background:'#fff',boxShadow:'0 6px 16px rgba(15,23,42,.06)'}}><div style={{display:'flex',justifyContent:'space-between',gap:10}}><div><strong>{row.item_name}</strong><div style={{fontSize:10,color:'#64748b',marginTop:3}}>Size {row.size} · {formatContractorMoney(row.pieces)} pieces · PKR {formatContractorMoney(row.total)}</div></div><small style={{color:'#94a3b8'}}>{new Intl.DateTimeFormat('en-PK',{timeZone:'Asia/Karachi',day:'numeric',month:'short',year:'numeric'}).format(new Date(row.occurred_at))}</small></div></article>)}</div></section></div>}</>}
src/features/contractor/components/ContractorWorkIntelligence.tsx:48:  const [loading, setLoading] = useState(true);
src/features/contractor/components/ContractorWorkIntelligence.tsx:112:      .wsi{position:relative;overflow:hidden;border:1px solid rgba(255,255,255,.94);border-radius:26px;padding:17px;background:linear-gradient(145deg,rgba(255,255,255,.92),rgba(239,246,255,.82) 54%,rgba(245,243,255,.9));box-shadow:0 26px 58px rgba(15,23,42,.09),0 8px 22px rgba(79,70,229,.06),inset 0 1px 0 rgba(255,255,255,.98);color:#172033}.wsi:before{content:"";position:absolute;inset:-80px auto auto 42%;width:260px;height:180px;border-radius:50%;background:radial-gradient(circle,rgba(34,211,238,.14),transparent 68%);pointer-events:none}.wsi-head{position:relative;display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.wsi-kicker{font-size:8px;font-weight:950;letter-spacing:.2em;color:#4f46e5}.wsi-title{margin:4px 0 0;font-size:21px;line-height:1.05;letter-spacing:-.045em;font-weight:950}.wsi-copy{margin:5px 0 0;max-width:540px;font-size:10px;line-height:1.45;color:#64748b;font-weight:750}.wsi-ranges{display:flex;gap:5px;padding:4px;border:1px solid rgba(148,163,184,.15);border-radius:13px;background:rgba(255,255,255,.55);box-shadow:inset 0 1px 0 #fff}.wsi-range{min-height:30px;padding:0 9px;border:0;border-radius:9px;background:transparent;color:#64748b;font:inherit;font-size:8px;font-weight:950;cursor:pointer}.wsi-range[aria-pressed=true]{background:linear-gradient(145deg,#eef2ff,#e0e7ff);color:#4338ca;box-shadow:0 5px 12px rgba(79,70,229,.1),inset 0 1px 0 #fff}.wsi-range:focus-visible{outline:2px solid rgba(99,102,241,.75);outline-offset:2px}.wsi-stage{position:relative;margin-top:14px;padding:10px 8px 6px;border:1px solid rgba(255,255,255,.82);border-radius:21px;background:linear-gradient(180deg,rgba(248,250,252,.64),rgba(239,246,255,.48));box-shadow:inset 0 1px 0 rgba(255,255,255,.92),inset 0 -12px 30px rgba(79,70,229,.025)}.wsi-chart{display:block;width:100%;height:auto;overflow:visible}.wsi-grid{stroke:rgba(100,116,139,.12);stroke-width:1}.wsi-axis{fill:#94a3b8;font-size:9px;font-weight:800}.wsi-taken-area{fill:url(#wsiTakenArea)}.wsi-done-area{fill:url(#wsiDoneArea)}.wsi-taken{fill:none;stroke:#4f46e5;stroke-width:3;stroke-linecap:round;stroke-linejoin:round;filter:drop-shadow(0 4px 6px rgba(79,70,229,.2))}.wsi-done{fill:none;stroke:#10b981;stroke-width:2.5;stroke-linecap:round;stroke-linejoin:round;filter:drop-shadow(0 3px 5px rgba(16,185,129,.18))}.wsi-point{stroke:#fff;stroke-width:2}.wsi-empty-chart{min-height:170px;display:grid;place-items:center;text-align:center;padding:22px}.wsi-empty-title{font-size:13px;font-weight:950;letter-spacing:-.02em}.wsi-empty-copy{margin:5px auto 0;max-width:390px;font-size:9px;line-height:1.5;color:#64748b;font-weight:750}.wsi-sparse-point{width:14px;height:14px;margin:0 auto 9px;border-radius:50%;background:linear-gradient(145deg,#4f46e5,#06b6d4);box-shadow:0 0 0 7px rgba(79,70,229,.09),0 8px 18px rgba(79,70,229,.2)}.wsi-legend{display:flex;gap:14px;flex-wrap:wrap;align-items:center;margin:4px 5px 0}.wsi-legend-item{display:flex;align-items:center;gap:6px;font-size:8px;color:#64748b;font-weight:900}.wsi-legend-line{width:19px;height:3px;border-radius:99px}.wsi-legend-line.taken{background:#4f46e5}.wsi-legend-line.done{background:#10b981}.wsi-event-count{margin-left:auto;color:#94a3b8}.wsi-signals{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:10px}.wsi-signal{display:flex;gap:8px;align-items:center;min-width:0;padding:9px 10px;border-radius:14px;border:1px solid rgba(255,255,255,.9);background:rgba(255,255,255,.58);box-shadow:inset 0 1px 0 #fff,0 8px 17px rgba(15,23,42,.035)}.wsi-signal-dot{width:7px;height:7px;flex:0 0 7px;border-radius:50%;background:#94a3b8;box-shadow:0 0 0 4px rgba(148,163,184,.1)}.wsi-signal-dot.positive{background:#10b981;box-shadow:0 0 0 4px rgba(16,185,129,.1)}.wsi-signal-dot.pressure{background:#f59e0b;box-shadow:0 0 0 4px rgba(245,158,11,.11)}.wsi-signal-label{font-size:7px;letter-spacing:.12em;text-transform:uppercase;color:#94a3b8;font-weight:950}.wsi-signal-value{margin-top:2px;font-size:11px;color:#334155;font-weight:950;overflow-wrap:anywhere}.wsi-foot{position:relative;display:grid;grid-template-columns:1fr auto;gap:10px;align-items:center;margin-top:9px;padding:10px 11px;border-radius:16px;background:linear-gradient(145deg,rgba(239,246,255,.72),rgba(245,243,255,.68));border:1px solid rgba(129,140,248,.13);box-shadow:inset 0 1px 0 rgba(255,255,255,.9),0 8px 18px rgba(79,70,229,.035);overflow:hidden}.wsi-foot.baseline{background:linear-gradient(135deg,rgba(238,242,255,.86),rgba(236,254,255,.72) 55%,rgba(245,243,255,.84));border-color:rgba(99,102,241,.2);box-shadow:inset 0 1px 0 rgba(255,255,255,.95),0 10px 22px rgba(79,70,229,.07)}.wsi-foot.baseline:before{content:"";position:absolute;right:-24px;top:-42px;width:120px;height:100px;border-radius:50%;background:radial-gradient(circle,rgba(34,211,238,.2),transparent 70%);pointer-events:none}.wsi-foot-cell{position:relative;display:flex;align-items:center;min-width:0}.wsi-foot-mark{width:8px;height:8px;flex:0 0 8px;margin-right:8px;border-radius:50%;background:#94a3b8;box-shadow:0 0 0 4px rgba(148,163,184,.1)}.wsi-foot-mark.positive{background:#10b981;box-shadow:0 0 0 4px rgba(16,185,129,.11)}.wsi-foot-mark.pressure{background:#f59e0b;box-shadow:0 0 0 4px rgba(245,158,11,.12)}.wsi-foot-mark.neutral{background:#6366f1;box-shadow:0 0 0 4px rgba(99,102,241,.1)}.wsi-momentum,.wsi-comparison{font-size:7px;color:#64748b;font-weight:950;letter-spacing:.12em;white-space:nowrap}.wsi-momentum strong,.wsi-comparison strong{margin-left:7px;color:#4338ca;font-size:11px;letter-spacing:0}.wsi-comparison{text-align:right}.wsi-comparison strong{color:#475569}.wsi-foot.baseline .wsi-comparison strong{color:#0f766e}.wsi-error{padding:20px;text-align:center;font-size:9px;color:#64748b;font-weight:800}.wsi-loading{min-height:240px;display:grid;place-items:center;font-size:9px;color:#64748b;font-weight:850}@media(max-width:700px){.wsi-head{flex-direction:column}.wsi-ranges{align-self:stretch}.wsi-range{flex:1}.wsi-signals{grid-template-columns:1fr 1fr}.wsi-signal:last-child{grid-column:1/-1}.wsi-foot{grid-template-columns:1fr;align-items:flex-start}.wsi-comparison{text-align:left}.wsi-foot.baseline{padding:10px}}@media(max-width:430px){.wsi{padding:13px;border-radius:22px}.wsi-title{font-size:19px}.wsi-stage{padding:7px 4px 4px}.wsi-axis{font-size:8px}.wsi-signals{grid-template-columns:1fr}.wsi-signal:last-child{grid-column:auto}.wsi-foot{padding:9px 10px}}@media(prefers-reduced-motion:reduce){.wsi *{scroll-behavior:auto!important;animation:none!important;transition:none!important}}
src/features/contractor/components/ContractorWorkIntelligence.tsx:115:    {loading ? <div className="wsi-stage wsi-loading" aria-label="Loading work intelligence">Reading real work movement…</div> : error ? <div className="wsi-stage wsi-error" role="alert">{error}</div> : <>
src/features/contractor/pages/ContractorAccountPage.tsx:23: const [profile,setProfile]=useState<Profile|null>(null),[account,setAccount]=useState<ContractorAccount|null>(null),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(''),[saved,setSaved]=useState(false),[commissionBased,setCommissionBased]=useState<boolean|null>(null),[workTypes,setWorkTypes]=useState<string[]>([]),[worksWith,setWorksWith]=useState<string[]>([]),[businessName,setBusinessName]=useState('');
src/features/contractor/pages/ContractorAccountPage.tsx:30: if(loading)return <main style={shell}><section style={{...card,marginTop:18}}><strong>Loading Contractor Account…</strong></section></main>;
src/features/contractor/pages/ContractorFinancePage.tsx:18: const[teamNumber,setTeamNumber]=useState<number|null>(null),[teamName,setTeamName]=useState<string|null>(null),[teamId,setTeamId]=useState<number|null>(null),[summary,setSummary]=useState<Summary|null>(null),[entries,setEntries]=useState<Entry[]>([]),[payments,setPayments]=useState<Payment[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[showAdd,setShowAdd]=useState(false),[amount,setAmount]=useState(''),[note,setNote]=useState(''),[submitting,setSubmitting]=useState(false),[submitError,setSubmitError]=useState(''),[success,setSuccess]=useState(''),[detail,setDetail]=useState<Detail>(null),[editing,setEditing]=useState<Payment|null>(null),[editAmount,setEditAmount]=useState(''),[editPaidAt,setEditPaidAt]=useState(''),[editNote,setEditNote]=useState(''),[editError,setEditError]=useState(''),[editSaving,setEditSaving]=useState(false),[deleting,setDeleting]=useState<Payment|null>(null),[deleteError,setDeleteError]=useState(''),[deleteSaving,setDeleteSaving]=useState(false);
src/features/contractor/pages/ContractorFinancePage.tsx:27: if(loading)return <main style={shell}><section style={{...card,marginTop:18}}><strong>Loading Contractor Finance…</strong></section></main>;
src/features/contractor/pages/ContractorOverviewPage.tsx:24: const[payload,setPayload]=useState<Payload|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[deletedTeamName,setDeletedTeamName]=useState('');
src/features/contractor/pages/ContractorOverviewPage.tsx:34:  {loading?<><div className="co5-card co5-skeleton"/><div className="co5-card co5-skeleton"/></>:error?<section className="co5-card co5-empty"><strong>Overview unavailable</strong><p>{error}</p><button className="co5-retry" type="button" onClick={()=>void load()}>Retry</button></section>:<>
src/features/contractor/pages/ContractorTeamDetailPage.tsx:15: const [rows,setRows]=useState<Row[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');
src/features/contractor/pages/ContractorTeamDetailPage.tsx:23: {loading?<><section className="ctd3state ctd3sk" aria-label="Loading team detail"/><section className="ctd3section"><div className="ctd3state ctd3sk" style={{height:126}}/></section></>:error?<section className="ctd3state" role="alert"><strong>Team detail unavailable</strong><p>{error}</p><button className="ctd3retry" type="button" onClick={()=>void load()}>Retry</button></section>:!rows.length?<section className="ctd3state"><strong>Team not found</strong><p>This team is no longer available in your Contractor workspace.</p><button className="ctd3retry" type="button" onClick={()=>navigate('/work/contractor?view=overview')}>Back to Overview</button></section>:<>
src/features/contractor/pages/ContractorTeamFinancePage.tsx:20: const [period,setPeriod]=useState<Period>('all'),[summary,setSummary]=useState(zero),[workers,setWorkers]=useState<Worker[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');
src/features/contractor/pages/ContractorTeamFinancePage.tsx:35: if(!teamId&&!loading)return <main className="tf-page"><div className="tf-action"><div className="tf-action-icon">!</div><h2>Team Finance unavailable</h2><p>{error||'The selected team context could not be resolved. Finance will not fall back to Contractor Overview.'}</p><div className="tf-action-buttons"><button className="tf-cancel" onClick={()=>navigate('/work/contractor?view=overview')}>Back to Overview</button></div></div></main>;
src/features/contractor/pages/ContractorTeamFinancePage.tsx:39: <div className="tf-section">WORKERS</div>{loading?<div className="tf-state">Loading real Team Finance…</div>:error?<div className="tf-state red">{error}</div>:!workers.length?<div className="tf-state">No approved worker membership records were found.</div>:<div className="tf-list">{workers.map(w=><article className="tf-worker" key={w.worker_profile_id}><div className="tf-id"><div className="tf-avatar">{w.avatar_url?<img src={w.avatar_url} alt=""/>:initials(w.display_name||w.username||'Worker')}</div><div><div className="tf-name">{w.display_name||w.username||'Work Social Worker'}</div><div className="tf-wid">Worker ID · {w.work_id||w.worker_profile_id}</div></div></div><div className="tf-nums"><div><label>PAYABLE</label><strong className="red">{money(w.payable)}</strong></div><div><label>PAID</label><strong className="green">{money(w.paid)}</strong></div><div><label>ADVANCE</label><strong className="orange">{money(w.advance_paid)}</strong></div><div><label>DUE</label><strong className="due">{money(w.due)}</strong></div></div><div className="tf-actions"><button className="tf-btn pay" onClick={()=>void open(w,'payment')}>View Payment</button><button className="tf-btn" onClick={()=>void open(w,'work')}>View Work</button></div></article>)}</div>}
src/features/contractor/pages/ContractorTeamPage.tsx:23:  const [team, setTeam] = useState<Team | null>(null), [leader, setLeader] = useState<Profile | null>(null), [entries, setEntries] = useState<TeamWorkEntry[]>([]), [totals, setTotals] = useState<TeamWorkTotals | null>(null), [trashCount, setTrashCount] = useState(0), [trashAttention, setTrashAttention] = useState(false), [loading, setLoading] = useState(true), [workLoading, setWorkLoading] = useState(true), [error, setError] = useState(''), [workError, setWorkError] = useState(''), [membersOpen, setMembersOpen] = useState(false), [addMembersOpen, setAddMembersOpen] = useState(false), [trashOpen, setTrashOpen] = useState(false), [originalEntryId, setOriginalEntryId] = useState<string | null>(null), [originalEntry, setOriginalEntry] = useState<EntryVersion | null>(null), [originalLoading, setOriginalLoading] = useState(false), [reportEntry, setReportEntry] = useState<TeamWorkEntry | null>(null), [reportNote, setReportNote] = useState(''), [reportSubmitting, setReportSubmitting] = useState(false), [reportSuccess, setReportSuccess] = useState(false), [reportError, setReportError] = useState(''), [historyPeriod, setHistoryPeriod] = useState<HistoryPeriod | null>(null);
src/features/contractor/pages/ContractorTeamPage.tsx:99:    {loading ? <div className="ctd-state"><strong>Loading Team Workspace…</strong><p style={{margin:'5px 0 0',color:'#64748b',fontSize:10}}>Loading your real team identity.</p></div> : error ? <div className="ctd-state"><div className="ctd-error">{error}</div><button type="button" className="ctd-retry" onClick={() => window.location.reload()}>Retry</button></div> : team ? <>
src/features/contractor/pages/ContractorTeamSettingsPage.tsx:10: const [team,setTeam]=useState<Team|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState('');
src/features/contractor/pages/ContractorTeamSettingsPage.tsx:13: if(loading)return <main style={shell}><section style={{...card,marginTop:18}}><strong>Loading Team Settings…</strong><p style={{margin:'6px 0 0',color:'#64748b',fontSize:12}}>Loading your real team configuration.</p></section></main>;
src/features/expense-manager/components/ExpenseFinancialIntelligenceCards.tsx:19: const[daily,setDaily]=useState<DailySpend[]>([]);const[selected,setSelected]=useState(0);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);
src/features/expense-manager/components/ExpenseFinancialIntelligenceCards.tsx:23: if(loading)return <div className="expense-trend__state">Loading persisted expense activity…</div>;
src/features/expense-manager/components/ExpenseOverviewDashboard.tsx:42:  const [loading, setLoading] = useState(true);
src/features/expense-manager/components/ExpenseOverviewDashboard.tsx:149:        .expense-overview__loading{grid-column:span 12;display:grid;gap:12px}.expense-overview__skeleton{height:126px;border-radius:20px;background:linear-gradient(90deg,#e2e8f0 25%,#f8fafc 50%,#e2e8f0 75%);background-size:200% 100%;animation:expense-shimmer 1.2s infinite}
src/features/expense-manager/components/ExpenseOverviewDashboard.tsx:165:        {loading && (<div className="expense-overview__loading" aria-label="Loading financial overview"><div className="expense-overview__skeleton" /><div className="expense-overview__skeleton" /></div>)}
src/features/expense-manager/components/ExpenseOverviewDashboard.tsx:166:        {!loading && error && <div className="expense-overview__error">{error}</div>}
src/features/expense-manager/components/ExpenseOverviewDashboard.tsx:167:        {!loading && !error && data && !data.has_financial_records && (<div className="expense-overview__card expense-overview__empty"><div className="expense-overview__empty-icon" aria-hidden="true">＋</div><h2 className="expense-overview__empty-title">No financial records yet</h2><p className="expense-overview__empty-copy">Start tracking your money by adding your first expense or income. Your overview will populate from persisted Expense Manager data.</p></div>)}
src/features/expense-manager/components/ExpenseOverviewDashboard.tsx:168:        {!loading && !error && data && data.has_financial_records && (
src/features/expense-manager/domain/accounts.ts:42:  if (input.openingBalance.trim() !== '' && !/^-?(?:\d+\.?\d*|\.\d+)$/.test(input.openingBalance.trim())) return 'Opening balance must be a valid number.';
src/features/expense-manager/domain/accounts.ts:44:  if (!Number.isFinite(openingBalance) || Math.abs(openingBalance) > 999999999999.99) return 'Opening balance is outside the supported range.';
src/features/expense-manager/pages/ExpenseAccountsPage.tsx:18:  const [loading, setLoading] = useState(true);
src/features/expense-manager/pages/ExpenseAccountsPage.tsx:81:    {loading ? <div className="expense-accounts__state" aria-live="polite"><h3>Loading accounts</h3><p>Fetching your persisted Expense Manager accounts and calculated balances.</p></div> : error ? <div className="expense-accounts__state" role="alert"><h3>Accounts could not be loaded</h3><p>{error}</p><button type="button" onClick={() => void load()}>Retry</button></div> : accounts.length === 0 ? <div className="expense-accounts__empty"><div className="expense-accounts__empty-icon" aria-hidden="true">＋</div><h3>No accounts yet</h3><p>Add the place where your money is held or spent. Your transactions and Overview will use the same persisted account.</p></div> : <div className="expense-accounts__grid">{accounts.map((account) => <article className="expense-account-card" key={account.id}><div className="expense-account-card__top"><div className="expense-account-card__identity"><span className="expense-account-card__icon" aria-hidden="true">{account.icon || accountTypeIcon(account.type)}</span><div><h3 className="expense-account-card__name">{account.name}</h3><p className="expense-account-card__type">{accountTypeLabel(account.type)}</p></div></div><ExpenseEntityActionMenu label={`More actions for ${account.name}`} actions={[{label:'Edit',onSelect:()=>openEdit(account)},{label:'Delete',onSelect:()=>remove(account),destructive:true,disabled:deletingId===account.id}]}/></div><span className="expense-account-card__balance-label">Current balance</span><p className="expense-account-card__balance">{money(account.balance, account.currency)}</p><div className="expense-account-card__meta"><span>{account.currency}</span><span>{account.transaction_count} {account.transaction_count === 1 ? 'transaction' : 'transactions'}</span></div></article>)}</div>}
src/features/expense-manager/pages/ExpenseAccountsPage.tsx:85:    {formOpen && <div className="expense-accounts__backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget && !saving) setFormOpen(false); }}><section className="expense-accounts__dialog" role="dialog" aria-modal="true" aria-labelledby="account-form-title"><div className="expense-accounts__dialog-head"><div><h3 id="account-form-title">{editing ? 'Edit account' : 'Add account'}</h3><p>{editing ? 'Update the persisted account without changing its transaction relationships.' : 'Set up a real account for fast transaction entry and accurate balances.'}</p></div><button type="button" className="expense-accounts__close" onClick={() => !saving && setFormOpen(false)} aria-label="Close account form">×</button></div><form className="expense-accounts__form" onSubmit={(event) => { event.preventDefault(); void save(); }}><div className="expense-accounts__field"><label htmlFor="expense-account-name">Account name</label><input id="expense-account-name" value={form.name} onChange={(event) => setForm((current) => ({ ...current, name: event.target.value }))} maxLength={80} autoFocus placeholder="e.g. Main Bank" /></div><div className="expense-accounts__field"><label htmlFor="expense-account-type">Account type</label><select id="expense-account-type" value={form.type} onChange={(event) => setForm((current) => ({ ...current, type: event.target.value as ExpenseAccountType }))}>{EXPENSE_ACCOUNT_TYPES.map((item) => <option key={item.value} value={item.value}>{item.icon} {item.label}</option>)}</select></div><div className="expense-accounts__two"><div className="expense-accounts__field"><label htmlFor="expense-account-opening">Opening balance</label><input id="expense-account-opening" inputMode="decimal" value={form.openingBalance} onChange={(event) => setForm((current) => ({ ...current, openingBalance: event.target.value }))} placeholder="0.00" /></div><div className="expense-accounts__field"><label htmlFor="expense-account-currency">Currency</label><input id="expense-account-currency" inputMode="text" autoCapitalize="characters" maxLength={3} value={form.currency} onChange={(event) => setForm((current) => ({ ...current, currency: event.target.value.toUpperCase() }))} placeholder="PKR" /></div></div>{formError && <div className="expense-accounts__error" role="alert">{formError}</div>}<div className="expense-accounts__actions"><button type="button" className="expense-accounts__cancel" onClick={() => !saving && setFormOpen(false)}>Cancel</button><button type="submit" className="expense-accounts__save" disabled={saving}>{saving ? 'Saving…' : editing ? 'Save changes' : 'Create account'}</button></div></form></section></div>}
src/features/expense-manager/pages/ExpenseActivityPage.tsx:38:  const [loading, setLoading] = useState(true);
src/features/expense-manager/pages/ExpenseActivityPage.tsx:56:    {loading && <div className="expense-activity__empty">Loading persisted activity…</div>}
src/features/expense-manager/pages/ExpenseActivityPage.tsx:57:    {!loading && error && <div className="expense-activity__error" role="alert">{error}</div>}
src/features/expense-manager/pages/ExpenseActivityPage.tsx:58:    {!loading && !error && !rows.length && <div className="expense-activity__empty">No persisted transactions in this period.</div>}
src/features/expense-manager/pages/ExpenseActivityPage.tsx:59:    {!loading && !error && rows.length > 0 && <div className="expense-activity__list">{rows.map((row) => { const sign = row.type === 'income' ? '+' : row.type === 'expense' ? '−' : '↔'; return <div className="expense-activity__row" key={row.id}><span className="expense-activity__icon" aria-hidden="true">{row.category_name ? row.category_name.slice(0, 1).toUpperCase() : row.type === 'transfer' ? '↔' : '•'}</span><div className="expense-activity__main"><div className="expense-activity__name">{row.category_name || (row.type === 'transfer' ? 'Transfer' : 'Uncategorised')}</div><div className="expense-activity__meta">{row.date} · {row.account_name || 'Account'}{row.note ? ` · ${row.note}` : ''}</div></div><strong className="expense-activity__amount" data-type={row.type}>{sign} {money(row.amount, row.account_currency || 'PKR')}</strong></div>; })}</div>}
src/features/expense-manager/pages/ExpenseBudgetsPage.tsx:3:export function ExpenseBudgetsPage({onNavigate:_onNavigate}:Props){const[uid,setUid]=useState(''),[budgets,setBudgets]=useState<ExpenseBudgetRecord[]>([]),[cats,setCats]=useState<ExpenseCategoryRecord[]>([]),[subs,setSubs]=useState<ExpenseSubcategoryRecord[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[modal,setModal]=useState(false),[editing,setEditing]=useState<ExpenseBudgetRecord|null>(null),[form,setForm]=useState<Form>({categoryId:'',subcategoryId:'',amount:'',month:cm()}),[saving,setSaving]=useState(false),[deleting,setDeleting]=useState(false),[menuId,setMenuId]=useState<string|null>(null),[notice,setNotice]=useState('');
src/features/expense-manager/pages/ExpenseBudgetsPage.tsx:6: const total=useMemo(()=>budgets.reduce((a,b)=>({limit:a.limit+b.budget_amount,spent:a.spent+b.spent}),{limit:0,spent:0}),[budgets]);if(loading)return <section className="budgets"><div className="state">Loading budgets…</div></section>;return <section className="budgets"><style>{`.budgets{max-width:1120px;margin:auto;padding:4px 2px 110px}.toolbar{display:flex;justify-content:space-between;gap:14px;align-items:center}.toolbar h2{margin:0;font-size:20px;font-weight:950}.toolbar p{margin:4px 0;color:#64748b;font-size:11px}.btn{min-height:40px;padding:0 12px;border:1px solid #dbe3ee;border-radius:12px;background:#fff;font:inherit;font-size:10px;font-weight:900;cursor:pointer}.primary{background:#2563eb;color:#fff;border-color:#2563eb}.toolbar>.btn.primary{min-height:46px;padding:0 17px;border-color:rgba(255,255,255,.7);border-radius:14px;background:linear-gradient(145deg,#2563eb 0%,#4f46e5 58%,#3730a3 100%);box-shadow:inset 0 1px 1px rgba(255,255,255,.4),inset 0 -3px 5px rgba(15,23,42,.22),0 8px 20px rgba(37,99,235,.28);font-weight:950;text-shadow:0 1px 1px rgba(15,23,42,.2);transition:transform .16s ease,box-shadow .16s ease,filter .16s ease}.toolbar>.btn.primary:hover{transform:translateY(-1px);filter:saturate(1.08) brightness(1.03);box-shadow:inset 0 1px 1px rgba(255,255,255,.48),inset 0 -3px 5px rgba(15,23,42,.22),0 12px 26px rgba(37,99,235,.34)}.toolbar>.btn.primary:active{transform:translateY(1px);box-shadow:inset 0 2px 5px rgba(15,23,42,.24),0 5px 12px rgba(37,99,235,.22)}.toolbar>.btn.primary:focus-visible{outline:2px solid rgba(37,99,235,.5);outline-offset:3px}.summary{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin:16px 0}.summary div,.card,.state{border:1px solid #e5eaf1;border-radius:18px;background:#fff;padding:14px}.summary span{display:block;color:#94a3b8;font-size:9px;font-weight:850}.summary strong{display:block;margin-top:5px;font-size:18px}.summary .spent strong{color:#dc2626}.list{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.cardhead{display:flex;justify-content:space-between;gap:8px}.title{font-size:14px;font-weight:950}.sub{margin-top:3px;color:#64748b;font-size:10px}.numbers{display:flex;justify-content:space-between;margin-top:14px;font-size:11px}.bar{height:8px;background:#e2e8f0;border-radius:99px;overflow:hidden;margin-top:9px}.fill{height:100%;background:#2563eb}.state{text-align:center;color:#64748b;font-size:11px}.menu-wrap{position:relative;flex:0 0 auto}.menu-trigger{width:36px;height:36px;display:grid;place-items:center;padding:0;border:1px solid #e2e8f0;border-radius:11px;background:linear-gradient(180deg,#fff,#f8fafc);color:#475569;font-size:18px;font-weight:950;line-height:1;box-shadow:inset 0 1px 0 rgba(255,255,255,.98),0 5px 12px rgba(15,23,42,.06);cursor:pointer;transition:transform .16s ease,box-shadow .16s ease,border-color .16s ease}.menu-trigger:hover{transform:translateY(-1px);border-color:rgba(37,99,235,.22);box-shadow:inset 0 1px 0 #fff,0 8px 17px rgba(15,23,42,.09)}.menu-trigger:active{transform:translateY(1px)}.menu-trigger:focus-visible{outline:2px solid rgba(37,99,235,.45);outline-offset:3px}.menu{position:absolute;right:0;top:42px;z-index:30;min-width:142px;padding:5px;border:1px solid #e2e8f0;border-radius:13px;background:rgba(255,255,255,.98);box-shadow:0 16px 35px rgba(15,23,42,.16),inset 0 1px 0 rgba(255,255,255,.98)}.menu button{width:100%;min-height:36px;padding:0 10px;border:0;border-radius:9px;background:transparent;color:#334155;text-align:left;font:inherit;font-size:10px;font-weight:900;cursor:pointer}.menu button:hover{background:#f1f5f9}.menu button.delete{color:#dc2626}.menu button.delete:hover{background:#fef2f2}.backdrop{position:fixed;inset:0;background:#0f172a66;z-index:1200;display:grid;place-items:end center;padding:10px}.modal{width:min(560px,100%);background:#fff;border-radius:22px;padding:18px}.field{display:grid;gap:6px;margin-top:12px}.field label{font-size:10px;font-weight:900}.field input,.field select{min-height:45px;border:1px solid #dbe3ee;border-radius:12px;padding:0 10px;font:inherit}.actions{display:flex;gap:8px;margin-top:15px}.actions button{flex:1;min-height:45px;border:1px solid #dbe3ee;border-radius:12px;font-weight:900}.actions button:last-child{background:#2563eb;color:#fff}.error{margin:10px 0;padding:10px;border-radius:10px;background:#fef2f2;color:#991b1b;font-size:10px}.notice{position:fixed;bottom:80px;left:50%;transform:translateX(-50%);z-index:1300;background:#0f172a;color:#fff;border-radius:99px;padding:10px 15px;font-size:11px}@media(max-width:700px){.toolbar{align-items:stretch;flex-direction:column}.toolbar>.btn.primary{width:100%}.list{grid-template-columns:1fr}.summary{grid-template-columns:1fr 1fr}.backdrop{padding:0}.modal{border-radius:22px 22px 0 0}}`}</style><div className="toolbar"><div><p>Budgets use the same real category/subcategory taxonomy as Transactions.</p></div><button className="btn primary" onClick={openAdd} disabled={!cats.length}>+ Add Budget</button></div>{error&&<div className="error">{error}</div>}{budgets.length>0&&<div className="summary"><div><span>Total limit</span><strong>{money(total.limit)}</strong></div><div className="spent"><span>Total spent</span><strong>{money(total.spent)}</strong></div></div>}{budgets.length===0?<div className="state">No budgets yet. Add a monthly category or subcategory budget.</div>:<div className="list">{budgets.map(b=>{const ratio=Math.min(budgetRatio(b.spent,b.budget_amount),1);const state=budgetState(b.spent,b.budget_amount);return <article className="card" key={b.id}><div className="cardhead"><div><div className="title">{b.category_name}</div><div className="sub">{b.subcategory_name??'Entire category'} · {b.start_date.slice(0,7)}</div></div><div className="menu-wrap"><button type="button" className="menu-trigger" aria-label={`Actions for ${b.category_name} budget`} aria-haspopup="menu" aria-expanded={menuId===b.id} onClick={()=>setMenuId(menuId===b.id?null:b.id)}>⋮</button>{menuId===b.id&&<div className="menu" role="menu"><button type="button" role="menuitem" onClick={()=>openEdit(b)}>Edit</button><button type="button" role="menuitem" className="delete" onClick={()=>void removeBudget(b)} disabled={deleting}>Delete</button></div>}</div></div><div className="numbers"><strong>{money(b.spent)} spent</strong><span>of {money(b.budget_amount)}</span></div><div className="bar"><div className="fill" style={{width:`${ratio*100}%`}}/></div><div className="sub">{state} · {money(budgetRemaining(b.spent,b.budget_amount))} remaining</div></article>})}</div>}{modal&&<div className="backdrop" onMouseDown={e=>{if(e.target===e.currentTarget&&!saving)setModal(false)}}><div className="modal"><h3>{editing?'Edit Budget':'Add Budget'}</h3><div className="field"><label>Category</label><select value={form.categoryId} onChange={e=>setForm({...form,categoryId:e.target.value,subcategoryId:''})}>{cats.map(c=><option key={c.id} value={c.id}>{c.icon?`${c.icon} `:''}{c.name}</option>)}</select></div><div className="field"><label>Subcategory (optional)</label><select value={form.subcategoryId} onChange={e=>setForm({...form,subcategoryId:e.target.value})}><option value="">Entire category</option>{filteredSubs.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></div><div className="field"><label>Monthly limit</label><input inputMode="decimal" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value.replace(/[^0-9.]/g,'')})}/></div><div className="field"><label>Month</label><input type="month" value={form.month} onChange={e=>setForm({...form,month:e.target.value})}/></div><div className="actions"><button onClick={()=>setModal(false)}>Cancel</button><button onClick={()=>void save()} disabled={saving}>{saving?'Saving…':'Save Budget'}</button></div></div></div>}{notice&&<div className="notice">✓ {notice}</div>}</section>}
src/features/expense-manager/pages/ExpenseCategoriesPage.tsx:19:  const [loading, setLoading] = useState(true);
src/features/expense-manager/pages/ExpenseCategoriesPage.tsx:58:  if (loading) return <section className="expense-categories"><div className="state">Loading your categories…</div></section>;
src/features/expense-manager/pages/ExpenseReportsPage.tsx:24:  const [month, setMonth] = useState(currentMonth); const [reloadKey, setReloadKey] = useState(0); const [data, setData] = useState<ExpenseReportsData | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState('');
src/features/expense-manager/pages/ExpenseReportsPage.tsx:34:    {loading && <div className="expense-reports__state" role="status"><h3>Loading reports</h3><p>Calculating this period from your persisted Expense Manager data.</p></div>}
src/features/expense-manager/pages/ExpenseReportsPage.tsx:35:    {!loading && error && <div className="expense-reports__state" role="alert"><h3>Reports could not be loaded</h3><p>{error}</p><button type="button" onClick={retry}>Retry</button></div>}
src/features/expense-manager/pages/ExpenseReportsPage.tsx:36:    {!loading && !error && data && !data.has_any_transactions && <div className="expense-reports__empty"><div className="expense-reports__empty-icon" aria-hidden="true">▥</div><h3>No transactions yet</h3><p>There is no persisted financial activity to analyze. Add your first expense, income, or transfer to start building reports.</p><button type="button" onClick={() => onNavigate('/expense-manager/transactions?intent=add')}>+ Add Transaction</button></div>}
src/features/expense-manager/pages/ExpenseReportsPage.tsx:37:    {!loading && !error && data && data.has_any_transactions && <>
src/features/expense-manager/pages/ExpenseTransactionsPage.tsx:19:  const [loading, setLoading] = useState(true);
src/features/expense-manager/pages/ExpenseTransactionsPage.tsx:96:    <style>{`.expense-transactions{width:min(1120px,100%);margin:0 auto;padding:0 2px 110px;box-sizing:border-box}.toolbar{display:flex;align-items:center;justify-content:space-between;gap:12px;margin:0 0 12px}.subtitle{margin:5px 0 0;color:#64748b;font-size:11px;font-weight:650}.add{position:relative;isolation:isolate;display:inline-flex;align-items:center;justify-content:center;gap:8px;min-height:46px;padding:0 18px;border:1px solid rgba(255,255,255,.72);border-radius:14px;background:linear-gradient(145deg,#2563eb 0%,#4f46e5 58%,#3730a3 100%);color:#fff;font:inherit;font-size:11px;font-weight:950;letter-spacing:.01em;cursor:pointer;box-shadow:inset 0 1px 1px rgba(255,255,255,.4),inset 0 -3px 5px rgba(15,23,42,.22),0 8px 20px rgba(37,99,235,.28);text-shadow:0 1px 1px rgba(15,23,42,.2);transition:transform .16s ease,box-shadow .16s ease,filter .16s ease}.add::before{content:'';position:absolute;inset:1px;border-radius:13px;background:linear-gradient(180deg,rgba(255,255,255,.2),transparent 42%);pointer-events:none;z-index:-1}.add:hover{filter:saturate(1.08) brightness(1.03);transform:translateY(-1px);box-shadow:inset 0 1px 1px rgba(255,255,255,.48),inset 0 -3px 5px rgba(15,23,42,.22),0 12px 26px rgba(37,99,235,.34)}.add:active{transform:translateY(1px);box-shadow:inset 0 2px 5px rgba(15,23,42,.24),0 5px 12px rgba(37,99,235,.22)}.add:focus-visible{outline:2px solid rgba(37,99,235,.55);outline-offset:3px}.period{width:fit-content;max-width:100%;margin:0 auto 10px;display:flex;align-items:center;justify-content:center;gap:5px;padding:5px;border:1px solid rgba(148,163,184,.2);border-radius:14px;background:rgba(255,255,255,.84);box-shadow:0 5px 14px rgba(15,23,42,.045),inset 0 1px 0 rgba(255,255,255,.9);backdrop-filter:blur(8px)}.period button{appearance:none;display:inline-flex;align-items:center;justify-content:center;flex:0 0 auto;width:38px;height:38px;min-height:38px;padding:0;border:0;border-radius:10px;background:transparent;color:#475569;font:inherit;font-size:21px;font-weight:700;line-height:1;cursor:pointer;transition:background-color .14s ease,color .14s ease,transform .1s ease,box-shadow .14s ease}.period button:hover{background:rgba(15,23,42,.055);color:#0f172a}.period button:active{transform:scale(.96);background:rgba(15,23,42,.08)}.period button:focus-visible{outline:2px solid rgba(37,99,235,.45);outline-offset:2px}.period-label{display:inline-flex!important;align-items:center;justify-content:center;gap:7px;min-width:126px;height:38px!important;padding:0 10px!important;border-radius:10px!important;color:#0f172a!important;background:transparent!important;font-size:13px!important;font-weight:800!important;letter-spacing:-.015em;white-space:nowrap}.period-label:hover{background:rgba(15,23,42,.035)!important}.period-label .period-icon{display:inline-flex;align-items:center;justify-content:center;width:17px;height:17px;color:#64748b;font-size:14px;line-height:1}.period-label .period-month{font-weight:850}.period-label .period-year{margin-left:1px;color:#64748b;font-size:12px;font-weight:650;letter-spacing:0}.filters{display:grid;grid-template-columns:1.6fr repeat(4,1fr);gap:7px;margin-bottom:10px}.input,.select{width:100%;min-height:43px;box-sizing:border-box;border:1px solid rgba(148,163,184,.2);border-radius:12px;background:#fff;color:#334155;padding:0 10px;font:inherit;font-size:11px;font-weight:700;outline:none}.filters .input{min-height:44px;padding:0 13px;border:1px solid rgba(99,102,241,.16);border-radius:13px;background:linear-gradient(180deg,#ffffff 0%,#f8fafc 100%);color:#172033;font-size:11px;font-weight:750;letter-spacing:.005em;box-shadow:inset 0 1px 0 rgba(255,255,255,.95),0 5px 14px rgba(15,23,42,.055);transition:border-color .16s ease,box-shadow .16s ease,transform .16s ease,background .16s ease}.filters .input::placeholder{color:#94a3b8;font-weight:650;opacity:1}.filters .input:hover{border-color:rgba(99,102,241,.28);box-shadow:inset 0 1px 0 rgba(255,255,255,.95),0 7px 17px rgba(15,23,42,.07)}.filters .input:focus{border-color:rgba(79,70,229,.46);background:#fff;box-shadow:inset 0 1px 0 rgba(255,255,255,.95),0 0 0 3px rgba(79,70,229,.08),0 8px 20px rgba(15,23,42,.08);transform:translateY(-1px)}.input:focus,.select:focus{border-color:rgba(37,99,235,.45);box-shadow:0 0 0 3px rgba(37,99,235,.08)}.context{display:flex;flex-wrap:wrap;gap:7px;margin:0 0 14px}.chip{display:inline-flex;align-items:center;gap:5px;min-height:28px;padding:0 9px;border:1px solid rgba(37,99,235,.15);border-radius:999px;background:#eff6ff;color:#1d4ed8;font-size:10px;font-weight:850}.chip button{border:0;background:transparent;color:inherit;font:inherit;cursor:pointer;padding:0}.group{margin-bottom:15px}.group-title{margin:0 0 6px 4px;color:#64748b;font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.08em}.list{overflow:hidden;border:1px solid rgba(148,163,184,.16);border-radius:18px;background:rgba(255,255,255,.9);box-shadow:0 10px 25px rgba(15,23,42,.05)}.row{width:100%;display:grid;grid-template-columns:44px minmax(0,1fr) auto;gap:11px;align-items:center;padding:12px;border:0;border-bottom:1px solid rgba(148,163,184,.11);background:transparent;text-align:left;cursor:pointer}.row:last-child{border-bottom:0}.row:hover{background:rgba(37,99,235,.035)}.mark{width:40px;height:40px;display:grid;place-items:center;border-radius:13px;background:rgba(37,99,235,.08);color:#2563eb;font-size:17px;font-weight:950}.main{min-width:0}.name{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#172033;font-size:12px;font-weight:900}.meta{display:block;margin-top:3px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#94a3b8;font-size:10px;font-weight:650}.amount{white-space:nowrap;color:#334155;font-size:12px;font-weight:950}.amount[data-type=income]{color:#047857}.amount[data-type=expense]{color:#b91c1c}.empty,.loading,.errorbox{padding:30px 18px;text-align:center;border:1px solid rgba(148,163,184,.16);border-radius:20px;background:rgba(255,255,255,.85)}.empty h3{margin:0;color:#172033;font-size:15px;font-weight:950}.empty p,.errorbox p{max-width:420px;margin:6px auto 14px;color:#64748b;font-size:11px;line-height:1.55;font-weight:650}.empty button,.retry{min-height:43px;padding:0 13px;border-radius:12px;border:1px solid rgba(37,99,235,.18);background:#2563eb;color:#fff;font:inherit;font-size:11px;font-weight:900;cursor:pointer}.toast{position:fixed;left:50%;bottom:84px;z-index:1300;transform:translateX(-50%);padding:10px 14px;border-radius:999px;background:rgba(15,23,42,.94);color:#fff;font-size:11px;font-weight:850;box-shadow:0 12px 28px rgba(15,23,42,.22)}.detail-backdrop{position:fixed;inset:0;z-index:1100;display:grid;place-items:end center;padding:10px;background:rgba(15,23,42,.4);backdrop-filter:blur(5px)}.detail{width:min(560px,100%);box-sizing:border-box;border:1px solid rgba(148,163,184,.18);border-radius:24px;background:#fff;padding:17px;box-shadow:0 28px 65px rgba(15,23,42,.23)}.detail-top{display:flex;justify-content:space-between;gap:12px}.detail h3{margin:0;color:#0f172a;font-size:20px;font-weight:950}.close{width:40px;height:40px;border:1px solid rgba(148,163,184,.18);border-radius:12px;background:#fff;color:#475569;font-size:20px;cursor:pointer}.detail-amount{margin:15px 0;padding:17px;border-radius:17px;background:linear-gradient(145deg,#f8fafc,#fff);font-size:28px;font-weight:950;letter-spacing:-.04em}.detail-grid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.detail-cell{padding:11px;border:1px solid rgba(148,163,184,.13);border-radius:13px}.detail-cell span{display:block;color:#94a3b8;font-size:9px;font-weight:850;text-transform:uppercase;letter-spacing:.08em}.detail-cell strong{display:block;margin-top:4px;color:#334155;font-size:11px;font-weight:850}.detail-actions{display:flex;gap:8px;margin-top:14px}.detail-actions button{flex:1;min-height:47px;border-radius:13px;font:inherit;font-size:11px;font-weight:900;cursor:pointer}.edit{border:1px solid rgba(37,99,235,.18);background:#2563eb;color:#fff}.delete{border:1px solid rgba(220,38,38,.16);background:#fff;color:#b91c1c}@media(max-width:800px){.filters{grid-template-columns:1fr 1fr}.filters .input{grid-column:1/-1}}@media(max-width:560px){.toolbar{align-items:flex-end}.add{min-height:44px;padding:0 15px;border-radius:13px}.filters{grid-template-columns:1fr}.filters .input{grid-column:auto}.period{max-width:100%;margin-left:auto;margin-right:auto}.period-label{min-width:116px}.detail-backdrop{padding:0}.detail{border-radius:23px 23px 0 0}}@media(max-width:340px){.add{min-height:42px;padding:0 13px;font-size:10px}.period{gap:2px;padding:4px}.period button{width:35px;height:36px;min-height:36px}.period-label{min-width:108px;height:36px!important;font-size:12px!important;padding:0 7px!important}.period-label .period-icon{width:15px}.period-label .period-year{font-size:11px}}`}</style>
src/features/expense-manager/pages/ExpenseTransactionsPage.tsx:120:    {loading ? <div className="loading">Loading persisted transactions…</div> : loadError ? <div className="errorbox"><p>{loadError}</p><button type="button" className="retry" onClick={() => void load()}>Retry</button></div> : grouped.length === 0 ? <div className="empty"><h3>{activeSubcategory ? `No transactions for ${activeSubcategory.name}` : activeCategory ? `No transactions for ${activeCategory.name}` : 'No transactions found'}</h3><p>The result is based on your persisted Expense Manager records and the filters currently selected.</p><button type="button" onClick={openAdd}>Add Transaction</button></div> : grouped.map(([date, rows]) => <div className="group" key={date}><h3 className="group-title">{new Intl.DateTimeFormat(undefined, { weekday: 'long', month: 'long', day: 'numeric' }).format(new Date(`${date}T00:00:00`))}</h3><div className="list">{rows.map((transaction) => <button type="button" className="row" key={transaction.id} onClick={() => setSelected(transaction)}><span className="mark">{transaction.category_icon || transactionSign(transaction.type)}</span><span className="main"><span className="name">{transaction.note || transaction.category_name || transaction.subcategory_name || (transaction.type === 'transfer' ? 'Transfer' : 'Transaction')}</span><span className="meta">{[transaction.category_name, transaction.subcategory_name, transaction.account_name].filter(Boolean).join(' · ') || 'Uncategorized'}</span></span><span className="amount" data-type={transaction.type}>{formatTransactionAmount(transaction.type, transaction.amount, transaction.account_currency)}</span></button>)}</div></div>)}
src/features/expense-manager/transactions-filter-popover.ts:133:if (document.readyState === 'loading') {
src/features/profile/components/AvatarUploader.tsx:16:  const [uploading, setUploading] = useState(false); const [error, setError] = useState<string | null>(null);
src/features/profile/components/AvatarUploader.tsx:42:    if (!selectedFile) return; setUploading(true); setError(null);
src/features/profile/components/AvatarUploader.tsx:50:    finally { setUploading(false); }
src/features/profile/components/AvatarUploader.tsx:72:    <button type="button" onClick={chooseFile} disabled={uploading}>📷 Choose profile photo</button>
src/features/profile/components/AvatarUploader.tsx:73:    {selectedFile && !uploading && <div>
src/features/profile/components/AvatarUploader.tsx:79:    {uploading && <p>Saving profile photo…</p>}
src/features/profile/components/ProfilePanel.tsx:66: const [editing,setEditing]=useState(false); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [error,setError]=useState<string|null>(null); const [saved,setSaved]=useState(false);
src/features/profile/components/ProfilePanel.tsx:99: if(loading)return <section className="premium-profile-panel"><style>{profilePremiumStyles}</style><section className="profile-state-card"><p>Loading profile…</p></section></section>;
src/features/worker/components/SalaryAllowanceSection.tsx:19:  const [open,setOpen]=useState(true); const [rows,setRows]=useState<Draft[]>([]); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [message,setMessage]=useState(''); const sectionRef=useRef<HTMLElement>(null); const [highlighted,setHighlighted]=useState(focused);
src/features/worker/components/SalaryAllowanceSection.tsx:21:  useEffect(()=>{if(!focused||loading)return;const timer=window.setTimeout(()=>{sectionRef.current?.scrollIntoView({behavior:'smooth',block:'center'});setHighlighted(true);window.setTimeout(()=>setHighlighted(false),3600)},180);return()=>window.clearTimeout(timer)},[focused,loading]);
src/features/worker/components/SalaryAllowanceSection.tsx:27:    {open&&<><div style={{padding:12,borderRadius:13,background:'linear-gradient(145deg,#f8fafc,#eef2ff)',color:'#475569',fontSize:11.5,lineHeight:1.5}}><strong style={{color:'#0f172a'}}>Better salary records</strong><br/>Answer four things: what it is, how much, how often, and when it can be lost. Conditions stay independent.</div>{loading?<div style={hint}>Loading your saved allowances…</div>:rows.map((row,index)=>{const threshold=row.eligibility_rule==='after_absences'||row.eligibility_rule==='after_unpaid_leaves';const typeOptions=allowanceTypes.map(type=>({value:type,label:type}));const freqOptions:Option[]=[{value:'monthly',label:'Monthly',description:'Included in the monthly salary cycle.'},{value:'other',label:'Other / one-off',description:'Record this as a non-monthly allowance.'}];return <div key={row.localId} style={{padding:13,borderRadius:16,border:'1px solid #e1e7ef',background:'rgba(255,255,255,.98)',display:'grid',gap:10,boxShadow:'0 5px 15px rgba(15,23,42,.045)'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:10}}><div style={{display:'flex',alignItems:'center',gap:8}}><span style={{width:26,height:26,borderRadius:9,display:'grid',placeItems:'center',background:'#eef2ff',color:'#4f46e5',fontSize:11,fontWeight:900}}>{index+1}</span><strong style={{color:'#0f172a',fontSize:13}}>Allowance {index+1}</strong></div><button type="button" onClick={()=>setRows(current=>current.filter(item=>item.localId!==row.localId))} style={{border:0,background:'#fef2f2',color:'#b91c1c',borderRadius:9,minHeight:32,padding:'0 9px',fontWeight:850,cursor:'pointer'}}>Remove</button></div><ChoicePopup label="Q1 · What is this allowance?" value={row.allowance_type} options={typeOptions} onChange={value=>update(row.localId,{allowance_type:value})}/><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10}}><label style={label}>Q2 · How much?<input type="number" min="0" step="0.01" value={row.amount||''} onChange={e=>update(row.localId,{amount:Number(e.target.value)})} style={input} placeholder="e.g. 3000"/></label><ChoicePopup label="Q3 · How often?" value={row.frequency} options={freqOptions} onChange={value=>update(row.localId,{frequency:value as SalaryAllowanceFrequency})}/></div><ChoicePopup label="Q4 · When should this allowance be paid or lost?" value={row.eligibility_rule} options={ruleOptions} onChange={value=>update(row.localId,{eligibility_rule:value as SalaryAllowanceRule,loss_after_count:null})}/>{row.eligibility_rule==='present_only'&&<div style={{padding:10,borderRadius:11,background:'#ecfdf5',color:'#047857',fontSize:11.5,fontWeight:750}}>Present = allowance paid. Absent = allowance becomes 0.</div>}{threshold&&<label style={label}>After how many {row.eligibility_rule==='after_absences'?'absences':'unpaid leaves'} should it be lost?<input type="number" min="1" step="1" value={row.loss_after_count??''} onChange={e=>update(row.localId,{loss_after_count:e.target.value?Number(e.target.value):null})} style={input} placeholder="e.g. 3"/></label>}{row.eligibility_rule==='custom'&&<label style={label}>What is the custom condition?<textarea rows={3} value={row.rule_note??''} onChange={e=>update(row.localId,{rule_note:e.target.value})} style={{...input,resize:'vertical'}} placeholder="Example: Pay only after completing 26 working days."/></label>}{row.eligibility_rule!=='custom'&&<label style={label}>Optional note / evidence<textarea rows={2} value={row.rule_note??''} onChange={e=>update(row.localId,{rule_note:e.target.value})} style={{...input,resize:'vertical'}} placeholder="Optional explanation for your salary record"/></label>}</div>})}<div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:9}}><button type="button" onClick={()=>setRows(current=>[...current,emptyDraft(effectiveFrom)])} style={{minHeight:43,borderRadius:12,border:'1px dashed #94a3b8',background:'#f8fafc',color:'#334155',fontWeight:900,cursor:'pointer'}}>＋ Add Allowance</button><button type="button" disabled={saving} onClick={save} style={{minHeight:43,borderRadius:12,border:0,background:'linear-gradient(145deg,#312e81,#0f172a)',color:'#fff',fontWeight:900,cursor:'pointer',opacity:saving?.7:1}}>{saving?'Saving…':'Save Allowances'}</button></div>{message&&<div role="status" style={{padding:11,borderRadius:11,background:message.includes('saved')||message.includes('configured')?'#ecfdf5':'#fef2f2',color:message.includes('saved')||message.includes('configured')?'#047857':'#991b1b',fontWeight:700,fontSize:12}}>{message}</div>}</>}
src/features/worker/components/WorkerFinance.tsx:32:  if (finance.loading) return <main style={{ width: '100%', maxWidth: 920, margin: '0 auto', padding: '24px 14px 112px' }}><p style={{ color: '#94a3b8' }}>Loading Worker Finance…</p></main>;
src/features/worker/components/WorkerFinance.tsx:39:    <section aria-labelledby="finance-history-heading"><div style={{ display: 'flex', alignItems: 'end', justifyContent: 'space-between', gap: 10, marginBottom: 9 }}><div><div style={{ color: '#64748b', fontSize: 10, textTransform: 'uppercase', letterSpacing: '.12em', fontWeight: 900 }}>{view === 'earnings' ? 'Work Entries only' : 'Received Amount history'}</div><h2 id="finance-history-heading" style={{ margin: '3px 0 0', fontSize: 19, letterSpacing: '-.02em', color: '#f8fafc' }}>{view === 'earnings' ? 'Earnings' : 'Received History'}</h2></div><span style={{ color: '#64748b', fontSize: 11 }}>{finance.entries.length} loaded</span></div>{finance.entries.length === 0 ? <div style={{ ...lightCard, padding: 26, textAlign: 'center' }}><div style={{ fontSize: 15, fontWeight: 900 }}>No records in this view</div><div style={{ marginTop: 6, color: '#94a3b8', fontSize: 12 }}>{view === 'earnings' ? 'No real Work Entries exist for the selected period.' : 'No active received or advance records exist yet.'}</div></div> : <div style={{ display: 'grid', gap: 9 }}>{finance.entries.map((entry) => entry.kind === 'earning' ? <EarningRow key={entry.id} entry={entry.workEntry} onDetails={openDetails} /> : <ReceivedRow key={entry.id} record={entry.record} onEdit={openEdit} onDelete={setDeleteTarget} />)}</div>}{finance.hasMore && <button type="button" onClick={() => void finance.loadMore()} disabled={finance.loadingMore} style={{ ...button, width: '100%', marginTop: 10, opacity: finance.loadingMore ? .6 : 1 }}>{finance.loadingMore ? 'Loading…' : 'Load More'}</button>}</section>
src/features/worker/components/WorkerFinance.tsx:40:    {modalOpen && <AddReceivedModal editing={editing} type={type} setType={setType} amount={amount} setAmount={setAmount} formError={formError} saving={finance.saving} onCancel={() => setModalOpen(false)} onSave={() => void submit()} />}{deleteTarget && <DeleteModal saving={finance.saving} onCancel={() => setDeleteTarget(null)} onDelete={() => void confirmDelete()} />}{(selectedEntry || detailsLoading || detailsError) && <WorkEntryDetails entry={selectedEntry} loading={detailsLoading} error={detailsError} onClose={() => { setSelectedEntry(null); setDetailsError(null); }} />}
src/features/worker/components/WorkerFinance.tsx:50:function WorkEntryDetails({ entry, loading, error, onClose }: { entry: WorkEntry | null; loading: boolean; error: string | null; onClose: () => void }) { return <div role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }} style={{ position: 'fixed', inset: 0, zIndex: 1250, display: 'flex', alignItems: 'flex-end', justifyContent: 'center', padding: '0 8px 8px', background: 'rgba(2,6,23,.64)', backdropFilter: 'blur(8px)' }}><section role="dialog" aria-modal="true" aria-labelledby="entry-details-title" style={{ width: '100%', maxWidth: 560, padding: 18, borderRadius: 22, border: '1px solid rgba(99,102,241,.2)', background: 'linear-gradient(145deg,#172033,#0f172a)', boxShadow: '0 -10px 42px rgba(0,0,0,.35)' }}><div style={{ display: 'flex', justifyContent: 'space-between', gap: 12 }}><h2 id="entry-details-title" style={{ margin: 0, color: '#f8fafc', fontSize: 18 }}>Work Entry details</h2><button type="button" onClick={onClose} style={{ ...button, minHeight: 34, width: 34, padding: 0 }}>×</button></div>{loading ? <p style={{ color: '#94a3b8' }}>Loading…</p> : error ? <p role="alert" style={{ color: '#fca5a5' }}>{error}</p> : entry ? <div style={{ marginTop: 14, display: 'grid', gap: 9, color: '#cbd5e1', fontSize: 12 }}><div><strong style={{ color: '#f8fafc' }}>{entry.item_name}</strong></div><div>Earnings: <strong style={{ color: '#67e8f9' }}>{amountLabel(entry.total)}</strong></div><div>Quantity: {formatWorkDecimal(entry.quantity)}</div><div>Rate: {amountLabel(entry.rate)}</div><div>Date: {dateLabel(entry.occurred_at)}</div>{entry.special_note && <div>Note: {entry.special_note}</div>}</div> : null}</section></div>; }
src/features/worker/components/WorkerHome.tsx:82:          <button className="worker-home__button worker-home__button--primary" type="button" onClick={() => setNewEntryOpen(true)} disabled={!dashboard.workerProfileId || dashboard.loading}>+ New Entry</button>
src/features/worker/components/WorkerHome.tsx:88:      {!dashboard.loading && !dashboard.workerProfileId && !dashboard.error ? (
src/features/worker/components/WorkerIdentityForm.tsx:39:    'Warehouse / Store': 'Describe your warehouse or store work. Example: stock handling, inventory, loading, or store management.',
src/features/worker/components/WorkerSettings.tsx:18:  const { workerProfile, profile, loading, error, reload } = useWorkerProfile(profileId ?? '');
src/features/worker/components/WorkerSettings.tsx:22:  if (loading) return shell(<section aria-live="polite" style={{ ...cardStyle, display: 'grid', gap: 8 }}><div aria-hidden="true" style={{ width: 58, height: 58, borderRadius: '50%', background: 'linear-gradient(145deg,#e0e7ff,#e2e8f0)', boxShadow: '0 10px 26px rgba(99,102,241,.16), inset 0 2px 4px rgba(255,255,255,.9)' }} /><strong style={{ color: '#172033' }}>Loading Worker profile…</strong><span style={{ ...mutedTextStyle, fontSize: 12 }}>Loading your real profile and Worker identity.</span></section>);
src/features/worker/components/WorkerTrash.tsx:16:  const [loading, setLoading] = useState(true);
src/features/worker/components/WorkerTrash.tsx:90:      {loading ? <section style={cardStyle}><p style={{ margin: 0, color: '#64748b' }}>Loading Trash…</p></section> : !entries.length ? <section style={cardStyle}><h2 style={{ margin: 0, fontSize: 18 }}>Trash is empty</h2><p style={{ margin: '7px 0 0', color: '#64748b' }}>Deleted Work Entries will appear here instead of being destroyed immediately.</p></section> : <div style={{ display: 'grid', gap: 12 }}>
src/features/worker/components/WorkerWorkEntryList.tsx:68:      .worker-entry-list__row{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:10px}.worker-entry-list__main{min-width:0}.worker-entry-list__name{display:flex;align-items:center;gap:7px;min-width:0;font-size:13px;font-weight:900;letter-spacing:-.015em}.worker-entry-list__name:before{content:'◈';display:grid;place-items:center;flex:0 0 23px;width:23px;height:23px;border:1px solid rgba(99,102,241,.18);border-radius:7px;background:linear-gradient(150deg,#fff,#e8edff 70%,#e0f7f5);color:#5148c8;font-size:9px;box-shadow:0 2px 4px rgba(15,23,42,.09),1px 1px 0 #fff inset,-1px -1px 2px rgba(79,70,229,.08) inset}.worker-entry-list__name-text{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.worker-entry-list__meta{display:block;margin:4px 0 0;padding-left:30px;color:#64748b;font-size:10px;line-height:1.35;font-weight:650;overflow-wrap:anywhere}.worker-entry-list__amount{display:inline-flex;align-items:center;justify-content:center;min-width:54px;padding:7px 8px;border:1px solid rgba(37,99,235,.18);border-radius:9px;background:linear-gradient(150deg,#fff,#edf4ff 65%,#e6faf7);color:#0f172a;font-size:16px;line-height:1;font-weight:950;letter-spacing:-.045em;box-shadow:0 1px 0 #fff inset,0 -2px 0 rgba(37,99,235,.08) inset,0 3px 5px rgba(15,23,42,.09),0 6px 10px rgba(37,99,235,.045);text-shadow:0 1px 0 #fff}.worker-entry-list__team-card{cursor:default;background:linear-gradient(150deg,#ffffff 0%,#f8fffd 58%,#eefbf8 100%);border-color:rgba(15,159,149,.18)}.worker-entry-list__team-card:hover{transform:none;border-color:rgba(15,159,149,.25)}.worker-entry-list__team-meta{display:flex;align-items:center;gap:6px;flex-wrap:wrap;margin:4px 0 0;padding-left:30px;color:#64748b;font-size:9.5px;line-height:1.35;font-weight:700}.worker-entry-list__team-name{color:#0f766e}.worker-entry-list__team-loading{padding:7px 10px;color:#64748b;font-size:10px}
src/features/worker/components/WorkerWorkEntryList.tsx:77:    {teamLoading && !hasTeam && <div className="worker-entry-list__team-loading">Loading Team Work history…</div>}
src/features/worker/hooks/useCurrentWorkerProfileId.ts:6:  const [loading, setLoading] = useState(true);
src/features/worker/hooks/useCurrentWorkerProfileId.ts:20:  return { profileId, loading, error };
src/features/worker/hooks/useWorkerDiary.ts:6:  const [entries, setEntries] = useState<WorkerDiaryEntry[]>([]); const [loading, setLoading] = useState(enabled); const [loadingMore, setLoadingMore] = useState(false); const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(null); const [search, setSearch] = useState(''); const [hasMore, setHasMore] = useState(false); const cursorRef = useRef<WorkerDiaryCursor | null>(null); const requestRef = useRef(0);
src/features/worker/hooks/useWorkerDiary.ts:14:  return {entries,loading,loadingMore,saving,error,search,setSearch,hasMore,loadMore:()=>load(search,true),refresh,create,update,remove,toggleTodo};
src/features/worker/hooks/useWorkerFinance.ts:29:  const session = useCurrentWorkerProfileId(); const [summary, setSummary] = useState<WorkerFinanceSummary>(EMPTY_SUMMARY); const [entries, setEntries] = useState<FinanceListEntry[]>([]); const [loading, setLoading] = useState(true); const [historyLoadingMore, setHistoryLoadingMore] = useState(false); const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(null); const historyRef = useRef<HistorySourceState>(emptyHistoryState()); const requestRef = useRef(0);
src/features/worker/hooks/useWorkerFinance.ts:38:  return useMemo(() => ({ ...session, summary, entries, loading: session.loading || loading, historyLoadingMore, historyHasMore: historyHasMore(historyRef.current), saving, error: session.error ?? error, hasEntries, reload: load, loadMoreHistory, addReceived, editReceived, removeReceived, restoreReceived }), [session, summary, entries, loading, historyLoadingMore, saving, error, hasEntries, load, loadMoreHistory, addReceived, editReceived, removeReceived, restoreReceived]);
src/features/worker/hooks/useWorkerFinance.ts:42:  const session = useCurrentWorkerProfileId(); const [summary, setSummary] = useState<WorkerFinanceSummary>(EMPTY_SUMMARY); const [entries, setEntries] = useState<FinanceListEntry[]>([]); const [loading, setLoading] = useState(true); const [loadingMore, setLoadingMore] = useState(false); const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(null); const [hasMore, setHasMore] = useState(false); const [lastDeletedId, setLastDeletedId] = useState<string | null>(null); const cursorRef = useRef<WorkHistoryCursor | null>(null); const requestRef = useRef(0);
src/features/worker/hooks/useWorkerFinance.ts:45:  const loadMore = useCallback(async () => { if (view !== 'earnings' || !session.profileId || loadingMore || !hasMore || !cursorRef.current) return; const requestId = requestRef.current; setLoadingMore(true); try { const result = await listWorkerFinanceEarnings(period === 'daily' ? 'day' : period === 'weekly' ? 'week' : 'month', 20, cursorRef.current, bounds); if (requestId !== requestRef.current) return; if (result.error) { setError(result.error.message); return; } setEntries((current) => [...current, ...result.data.map(toEarningEntry)]); setHasMore(result.count > result.data.length); if (result.data.length) cursorRef.current = { occurred_at: result.data[result.data.length - 1].occurred_at, id: result.data[result.data.length - 1].id }; } finally { if (requestId === requestRef.current) setLoadingMore(false); } }, [bounds, hasMore, loadingMore, period, session.profileId, view]);
src/features/worker/hooks/useWorkerFinance.ts:51:  return useMemo(() => ({ ...session, summary, entries, loading: session.loading || loading, loadingMore, saving, error: session.error ?? error, hasMore, lastDeletedId, loadMore, addReceived, editReceived, removeReceived, restoreReceived, reload: refresh }), [session, summary, entries, loading, loadingMore, saving, error, hasMore, lastDeletedId, loadMore, addReceived, editReceived, removeReceived, restoreReceived, refresh]);
src/features/worker/hooks/useWorkerProfile.ts:9:  const [loading, setLoading] = useState(Boolean(profileId));
src/features/worker/hooks/useWorkerProfile.ts:79:  return { workerProfile, profile, loading, saving, error, reload: load, save };
src/features/worker/hooks/useWorkerWorkDashboard.ts:15:  const [loading, setLoading] = useState(true);
src/features/worker/hooks/useWorkerWorkDashboard.ts:45:  return { workerProfileId, totals, periodLabels, loading, saving, error, reload: load, createEntry };
src/features/worker/hooks/useWorkerWorkHistory.ts:11:  const [loading, setLoading] = useState(true);
src/features/worker/hooks/useWorkerWorkHistory.ts:12:  const [loadingMore, setLoadingMore] = useState(false);
src/features/worker/hooks/useWorkerWorkHistory.ts:20:    if (append && loadingMore) return;
src/features/worker/hooks/useWorkerWorkHistory.ts:33:  }, [entries, loadingMore, period, selectedBounds]);
src/features/worker/hooks/useWorkerWorkHistory.ts:57:  return { entries, hasMore, loading, loadingMore, actionError, selectedEntry, versions, versionsLoading, openDetails, closeDetails: () => setSelectedEntry(null), editEntry, trashEntry, restoreEntry, removePermanently, reloadSelectedEntry, loadMore: () => load(true), refresh };
src/features/worker/pages/SalaryDashboardPage.tsx:26:  const { workerProfile, loading } = useWorkerProfile(profileId);
src/features/worker/pages/SalaryDashboardPage.tsx:54:  if (loading) return <main style={{ padding: 24 }}>Loading Salary Dashboard…</main>;
src/features/worker/pages/SalaryFinancePage.tsx:25:  const { workerProfile, loading: profileLoading } = useWorkerProfile(profileId);
src/features/worker/pages/SalaryFinancePage.tsx:30:  const [loading, setLoading] = useState(true);
src/features/worker/pages/SalaryFinancePage.tsx:70:  if (profileLoading || loading) return <main style={shell}><p style={{ color: '#64748b' }}>Loading Salary Finance…</p></main>;
src/features/worker/pages/SalarySetupPage.tsx:39:  const { workerProfile, loading, error } = useWorkerProfile(profileId);
src/features/worker/pages/SalarySetupPage.tsx:76:    if (loading || error || !workerProfile) return;
src/features/worker/pages/SalarySetupPage.tsx:100:  }, [profileId, loading, error, workerProfile]);
src/features/worker/pages/SalarySetupPage.tsx:103:    if ((!focusSalaryType && !focusAllowances) || !privacyAccepted || loading || error || !workerProfile) return;
src/features/worker/pages/SalarySetupPage.tsx:106:  }, [focusSalaryType, focusAllowances, privacyAccepted, loading, error, workerProfile]);
src/features/worker/pages/SalarySetupPage.tsx:108:  if (loading) return <main className="salary-setup-page" style={{ padding: 24 }}>Loading Salary Setup…</main>;
src/features/worker/pages/WorkerDiaryPage.tsx:76:  if (session.loading) return <main style={pageStyle}><p>Loading your private diary…</p></main>; if (session.error || !session.profileId) return <main style={pageStyle}><p role="alert">{session.error ?? 'Authenticated Worker is unavailable.'}</p></main>;
src/features/worker/pages/WorkerDiaryPage.tsx:88:    </section> : <section>{diary.loading ? <div style={emptyCard}>Loading diary…</div> : diary.entries.length === 0 ? <div style={emptyCard}>Nothing here yet. Use <strong>+ Add</strong> to create your first private entry.</div> : <div style={{ display: 'grid', gap: 10 }}>{diary.entries.map(entry => <article key={entry.id} style={entryCard}><button type="button" onClick={() => setViewing(entry)} style={entryMain}><span style={{ fontSize: 24 }}>{TYPE_META[entry.entry_type][0]}</span><span style={{ minWidth: 0, flex: 1, textAlign: 'left' }}><small style={metaStyle}>{TYPE_META[entry.entry_type][1]} · {formatDisplayDate(entry.created_at, timezone, 'gregory', { month: 'short', day: 'numeric', hour: 'numeric', minute: '2-digit' })}</small>{entry.title && <strong style={entryTitle}>{entry.title}</strong>}<span style={{ ...contentStyle, textDecoration: entry.entry_type === 'todo' && entry.completed ? 'line-through' : undefined }}>{entry.content}</span>{entry.entry_type === 'event' && entry.event_start_at && <small style={reminderText}>📅 {formatDisplayDate(entry.event_start_at, entry.event_timezone ?? timezone, calendar, { weekday: 'short', month: 'short', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit' })}</small>}{entry.reminder?.enabled && <small style={reminderText}>🔔 {formatDisplayDate(entry.reminder.scheduled_at, entry.reminder.timezone, 'gregory', { weekday: 'short', month: 'short', day: 'numeric', hour: 'numeric', minute: '2-digit' })}</small>}</span></button><div style={actionsStyle}>{entry.entry_type === 'todo' && <button type="button" onClick={() => void diary.toggleTodo(entry.id, !entry.completed)} style={actionStyle}>{entry.completed ? '↶ Uncomplete' : '✓ Complete'}</button>}<button type="button" onClick={() => openEdit(entry)} style={actionStyle}>Edit</button><button type="button" onClick={() => setDeleteTarget(entry)} style={{ ...actionStyle, color: '#b91c1c' }}>Delete</button></div></article>)}</div>}</section>}
src/features/worker/pages/WorkerFinancePage.tsx:9:  const { workerProfile, loading } = useWorkerProfile(profileId);
src/features/worker/pages/WorkerFinancePage.tsx:11:  if (loading) return <main style={{ padding: 24 }}>Loading Worker finance…</main>;
src/features/worker/pages/WorkerFinancePage.tsx:18:  const [loading, setLoading] = useState(true);
src/features/worker/pages/WorkerFinancePage.tsx:20:  if (loading) return <main style={{ padding: 24 }}>Loading Worker finance…</main>;
src/features/worker/pages/WorkerIdentityPage.tsx:13:  const { workerProfile, profile, loading, saving, error, save, reload } = useWorkerProfile(profileId);
src/features/worker/pages/WorkerIdentityPage.tsx:18:  if (loading) return <main style={{ padding: 24 }}>Loading Work Identity…</main>;
src/features/worker/pages/WorkerOverviewPage.tsx:24: const [now,setNow]=useState(new Date());const [recent,setRecent]=useState<OverallEntry[]>([]);const [v,setV]=useState<Record<string,V>>({today:Z,yesterday:Z,week:Z,lastWeek:Z,month:Z,lastMonth:Z});const [teams,setTeams]=useState<Team[]>([]);const [teamFin,setTeamFin]=useState<Record<number,{paid:number;due:number}>>({});const [loading,setLoading]=useState(true);const [error,setError]=useState('');
src/features/worker/pages/WorkerOverviewPage.tsx:34:`}</style><div className="wo-shell"><section className="wo-hero"><div className="wo-brand"><div><b>WS · Work Social</b><small>YOUR PERSONAL WORK SPACE</small></div><button className="wo-set" onClick={()=>navigate('/work/settings')}>⚙ Settings</button></div><div className="wo-k">WORKER · PERSONAL COMMAND CENTER</div><h1>Welcome back, <span>{name}</span></h1><p className="wo-copy">Personal work, earnings and real team context — one command center.</p><div className="wo-clock"><span className="wo-live"><i/>LIVE</span><span className="wo-time">{time}</span></div><div className="wo-ticker"><div>{date} · {num(personalMonth)} PKR PERSONAL WORK VALUE THIS MONTH · {teams.length} REAL TEAM{teams.length===1?'':'S'} · WORK SOCIAL</div></div></section>{loading?<section className="wo-card"><div className="wo-empty">Loading real Worker data…</div></section>:<>{error&&<div className="wo-error">{error}</div>}<section className="wo-card"><div className="wo-eyebrow">01 · LIVING WORK FIELD</div><h2>Your work, alive in the field</h2><p className="wo-muted">Five latest real Worker work records become a living constellation. Quantity, source and date stay real; the field is only the visual layer.</p>{recent.length?<div className="wo-field"><div className="wo-field-label">LIVE WORK FIELD · 5 REAL SIGNALS</div><div className="wo-field-status">● CONNECTED</div><div className="wo-field-grid"/><span className="wo-field-star s1"/><span className="wo-field-star s2"/><span className="wo-field-star s3"/><span className="wo-field-star s4"/><span className="wo-field-star s5"/><span className="wo-field-star s6"/><div className="wo-field-lines"><span/><span/><span/><span/><span/></div><div className="wo-field-core"><small>THIS MONTH</small><strong>{num(v.month.quantity)}</strong><em>PCS · REAL WORK</em></div>{recent.map((x,i)=><div className={`wo-star-node n${i+1}`} key={`${x.source}-${x.id}`}><i/><b>{num(Number(x.quantity))} PCS</b><small>{x.team_number?`TEAM T${x.team_number}`:'PERSONAL'} · {new Date(x.occurred_at).toLocaleDateString(undefined,{month:'short',day:'numeric'})}</small></div>)}<div className="wo-field-footer"><span>PERSONAL + AUTHORIZED TEAM WORK</span><b>{v.month.count} REAL ENTRIES THIS MONTH</b></div></div>:<div className="wo-empty">No Worker work records available.</div>}<div className="wo-stats"><div className="wo-stat cyan"><small>OVERALL MONTH VOLUME</small><strong>{num(v.month.quantity)} pcs</strong></div><div className="wo-stat violet"><small>OVERALL MONTH VALUE</small><strong>{money(v.month.earning)}</strong></div><div className="wo-stat lime"><small>REAL ENTRIES</small><strong>{v.month.count}</strong></div></div></section><section className="wo-card"><div className="wo-eyebrow">02 · WORK VOLUME PRISM</div><h2>Your work, in light</h2><p className="wo-muted">Real Worker work volume across personal work and authorized teams — rendered as one living prism. The light is visual; every quantity shown is real.</p><div className="wo-prism" aria-label="Real Worker work volume prism"><div className="wo-prism-grid"/><div className="wo-prism-core"><span/><span/></div><div className="wo-prism-value"><small>THIS MONTH · OVERALL VOLUME</small><strong>{num(v.month.quantity)}</strong><em>PCS · PERSONAL + AUTHORIZED TEAM WORK</em></div>{vol.map(([label,x])=>{const intensity=x.quantity>0?Math.max(.24,Math.min(1,.28+.72*(x.quantity/maxVolume))):.12;return <div className="wo-prism-facet" key={label} style={{opacity:intensity}}><b>{label}</b><strong>{num(x.quantity)} PCS</strong></div>})}<div className="wo-prism-meta"><span>SIX REAL PERIODS · VISUAL INTENSITY FOLLOWS QUANTITY</span><b>PEAK · {peak[0]} · {num(peak[1].quantity)} PCS</b></div></div></section><section className="wo-card"><div className="wo-eyebrow">03 · WORK → EARNING → RECEIVED</div><h2>Financial result of your work</h2><p className="wo-muted">Existing Worker Finance summary is authoritative; Overview does not rewrite accounting.</p><div className="wo-fin"><div className="wo-finbox"><small>WORK / EARNINGS</small><strong>{money(finance.summary.total_earnings)}</strong></div><span className="wo-arrow">→</span><div className="wo-finbox"><small>EARNED</small><strong>{money(finance.summary.total_earnings)}</strong></div><span className="wo-arrow">→</span><div className="wo-finbox"><small>RECEIVED</small><strong>{money(finance.summary.received)}</strong></div></div><div className="wo-rem">Remaining: <b>{money(finance.summary.remaining)}</b></div><div className="wo-actions"><button className="wo-btn primary" onClick={()=>navigate('/work/dashboard')}>My Work</button><button className="wo-btn" onClick={()=>navigate('/work/finance')}>Personal Finance</button></div></section><WorkerOverallHealthCard todayEntries={v.today.count} weekEntries={v.week.count} remaining={finance.summary.remaining}/><section className="wo-card"><div className="wo-eyebrow">MY TEAMS · REAL CONTEXT</div><h2>Team workspaces</h2><p className="wo-muted">Your approved teams, each with its own real team workspace.</p>{teams.length?<div className="wo-teams">{teams.map(t=>{const f=teamFin[t.team_id];return <div className="wo-team" key={t.team_id}><div><b>{t.team_name}</b><small>TEAM {t.team_number} · {t.team_purpose||'Approved membership'}</small>{f&&<em>Month finance · Paid {money(f.paid)} · Due {money(f.due)}</em>}</div><div className="wo-team-actions"><button className="wo-mini" onClick={()=>navigate(`/work/team-work/${t.team_number}`)}>VISIT TEAM →</button></div></div>})}</div>:<div className="wo-empty">No approved team memberships found.</div>}</section><section className="wo-card"><div className="wo-eyebrow">PERSONAL ENTRY</div><h2>Existing Worker tools</h2><p className="wo-muted">These remain the existing Worker Dashboard and Finance pages; Overview is only the central entry layer.</p><div className="wo-actions"><button className="wo-btn primary" onClick={()=>navigate('/work/dashboard')}>Worker Dashboard</button><button className="wo-btn" onClick={()=>navigate('/work/finance')}>Worker Finance</button></div></section></>}</div></main>;
src/features/worker/pages/WorkerTeamDashboardPage.tsx:44:  const [loading, setLoading] = useState(true);
src/features/worker/pages/WorkerTeamDashboardPage.tsx:134:  if (loading) return <main className="team-dashboard-page"><div className="td-state"><strong>Opening Team Dashboard…</strong><p>Checking your approved membership for this team.</p></div></main>;
src/features/worker/pages/WorkerTeamDashboardPage.tsx:165:      {showExitReason && <div className="td-modal-backdrop" role="dialog" aria-modal="true" aria-label="Leave team reason"><section className="td-modal"><div className="td-modal-badge">FINAL TEAM EXIT · {team.team_number}</div><h3>Leave {team.team_name}?</h3><p>Reason is optional. Submit to leave the team and remove your approved Team Workspace access.</p><textarea className="td-reason" value={reason} onChange={event => setReason(event.target.value)} maxLength={1000} placeholder="Reason (optional)" disabled={leaving}/>{leaveError && <p className="td-error-text">{leaveError}</p>}<div className="td-modal-actions"><button type="button" className="td-modal-btn td-cancel" onClick={() => { if (!leaving) { setShowExitReason(false); setLeaveError(''); } }} disabled={leaving}>Cancel</button><button type="button" className="td-modal-btn td-confirm-exit" onClick={() => void submitLeave()} disabled={leaving}>{leaving ? 'Leaving…' : 'Submit & Leave Team'}</button></div></section></div>}
src/features/worker/pages/WorkerTeamDashboardWorkPage.tsx:43:  const [loading, setLoading] = useState(true);
src/features/worker/pages/WorkerTeamDashboardWorkPage.tsx:105:  if (loading) return <main className="twd"><section className="twd-state"><strong>Opening Team Dashboard…</strong><p>Checking your approved membership and Team Work data.</p></section></main>;
src/features/worker/pages/WorkerTeamDashboardWorkPage.tsx:119:    <section className="twd-hero"><h2 className="twd-name">{context.team_name}</h2><div className="twd-meta">TEAM ID · {context.team_number} · APPROVED MEMBER</div><p className="twd-purpose">{context.team_purpose || 'Your persisted work inside this approved team workspace.'}</p><div className="twd-actions"><button type="button" className="twd-button twd-primary" onClick={() => setModal('new')}>+ Add New Entry</button><button type="button" className="twd-button" onClick={() => { setPeriod('lifetime'); document.getElementById('team-work-history')?.scrollIntoView({ behavior: 'smooth' }); }}>Work History</button><button type="button" className="twd-button" onClick={() => navigate(`/work/team-work/${context.team_number}/settings`)}>Team Settings</button></div></section>
src/features/worker/pages/WorkerTeamDashboardWorkPageV2.tsx:21: const [context,setContext]=useState<Context|null>(null);const [totals,setTotals]=useState(EMPTY);const [entries,setEntries]=useState<Entry[]>([]);const [trash,setTrash]=useState<Entry[]>([]);const [reports,setReports]=useState<Report[]>([]);const [highlightedEntryId,setHighlightedEntryId]=useState<string|null>(null);const [period,setPeriod]=useState<Period>('lifetime');const [loading,setLoading]=useState(true);const [error,setError]=useState('');const [modal,setModal]=useState<'new'|'edit'|null>(null);const [editing,setEditing]=useState<Entry|null>(null);const [saving,setSaving]=useState(false);const [busy,setBusy]=useState<string|null>(null);const [showTrash,setShowTrash]=useState(false);const [confirmingDelete,setConfirmingDelete]=useState<string|null>(null);const [versions,setVersions]=useState<EntryVersion[]>([]);const [versionEntry,setVersionEntry]=useState<string|null>(null);const [editedEntryIds,setEditedEntryIds]=useState<Set<string>>(new Set());
src/features/worker/pages/WorkerTeamDashboardWorkPageV2.tsx:36: if(loading)return <main className="twd"><section className="twd-state"><strong>Opening Team Dashboard…</strong><p>Checking approved membership and Team Work data.</p></section></main>;
src/features/worker/pages/WorkerTeamFinancePage.tsx:34:  const [loading, setLoading] = useState(true);
src/features/worker/pages/WorkerTeamFinancePage.tsx:105:  if (loading) return <main className="wtf-page"><div className="wtf-state">Opening Team Finance…</div></main>;
src/features/worker/pages/WorkerTeamWorkPage.tsx:25:  const [loading, setLoading] = useState(true);
src/features/worker/pages/WorkerTeamWorkPage.tsx:81:        <section className="wtw-hero" aria-label="Team Work introduction"><div className="wtw-hero-grid"><div className="wtw-hero-copy"><h2 className="wtw-hero-title">Approved team workspace</h2><p>Only teams where your Worker account is an active member appear here. Personal My Work totals stay separate.</p></div><div className="wtw-count" aria-label={`${teams.length} approved teams`}><strong>{teams.length}</strong><span>Teams</span></div></div></section>
src/features/worker/pages/WorkerTeamWorkPage.tsx:83:        {loading ? <div className="wtw-state"><strong>Loading your teams…</strong><p>Checking your real approved Team Work memberships.</p></div> : error ? <div className="wtw-state"><div className="wtw-error">{error}</div><button type="button" className="wtw-refresh" onClick={() => window.location.reload()}>Retry</button></div> : teams.length === 0 ? <div className="wtw-state"><strong>No approved teams yet</strong><p>When a Contractor invitation is accepted, that real team will appear here automatically.</p></div> : <div className="wtw-grid">{teams.map(team => <article className="wtw-card" key={team.team_id}><div className="wtw-card-head"><div className="wtw-team-identity"><div className="wtw-avatar">{team.leader_avatar_url ? <img src={team.leader_avatar_url} alt="Team owner"/> : initials(team.team_name)}</div><div style={{minWidth:0}}><h2 className="wtw-team-name">{team.team_name}</h2><div className="wtw-team-id">TEAM ID · {team.team_number}</div></div></div><span className="wtw-status">Approved</span></div><p className="wtw-purpose">{team.team_purpose}</p><div className="wtw-meta"><span>Team Owner</span><strong>{team.leader_display_name || team.leader_username || 'Work Social Contractor'}</strong></div><button type="button" className="wtw-card-button" onClick={() => navigate(`/work/team-work/${team.team_number}`)}>Open Team Work&nbsp;→</button></article>)}</div>}
src/features/worker/pages/WorkerWorkHistoryPage.tsx:45:  if (session.loading) return <main className="worker-history"><section className="worker-history__state"><span className="worker-history__state-mark" aria-hidden="true">◌</span><p>Loading Work History…</p></section></main>;
src/features/worker/pages/WorkerWorkHistoryPage.tsx:78:    <section aria-labelledby="worker-history-list-heading"><div className="worker-history__list-title"><h2 id="worker-history-list-heading">Work Entries</h2>{!history.loading && history.entries.length > 0 && <span>{history.entries.length}{history.hasMore ? '+' : ''} loaded</span>}</div>{history.actionError && <p className="worker-history__state worker-history__state--error" role="alert"><span className="worker-history__state-mark" aria-hidden="true">!</span><span>{history.actionError}</span></p>}{history.loading ? <section className="worker-history__state"><span className="worker-history__state-mark" aria-hidden="true">◌</span><p>Loading Work History…</p></section> : history.entries.length === 0 ? <section className="worker-history__state" style={{display:'block',textAlign:'center'}}><span className="worker-history__state-mark" aria-hidden="true">○</span><h3 style={{margin:'7px 0 0',fontSize:15,color:'#172033'}}>No Work Entries yet</h3><p style={{margin:'4px 0 0'}}>Persisted Work Entries will appear here when they exist.</p></section> : <><WorkerWorkEntryList entries={history.entries} onOpen={(entry) => void history.openDetails(entry)} />{history.hasMore && <button className="worker-history__more" type="button" onClick={() => void history.loadMore()} disabled={history.loadingMore}>{history.loadingMore ? 'Loading more…' : 'Load More'}</button>}</>}</section>
src/features/worker/pages/WorkerWorkHousePage.tsx:23:  if (session.loading || profile.loading) return <main style={{ width: '100%', maxWidth: 900, margin: '0 auto', padding: '24px 14px 112px', boxSizing: 'border-box' }}><p style={{ color: '#64748b' }}>Loading Worker workspace…</p></main>;
