import {supabase} from '../../../lib/supabase/client';
import type {ExpenseAccountOption,ExpenseCategoryOption,ExpenseTransactionInput,ExpenseTransactionRecord} from '../domain/transactions';
import {toMoney} from '../domain/transactions';
type TransactionRow={id:string;user_id:string;type:'expense'|'income'|'transfer';amount:number|string;account_id:string|null;category_id:string|null;subcategory_id:string|null;from_account_id:string|null;to_account_id:string|null;date:string;note:string|null;created_at:string;updated_at:string};
type AccountRow={id:string;name:string;type:string;currency:string;icon:string|null};type CategoryRow={id:string;name:string;type:'expense'|'income';icon:string|null;color:string|null};type SubRow={id:string;name:string;category_id:string};
export async function loadExpenseTransactionData(userId:string,options:{fromDate?:string;toDate?:string}={}):Promise<{transactions:ExpenseTransactionRecord[];accounts:ExpenseAccountOption[];categories:ExpenseCategoryOption[]}>{let q=supabase.from('expense_transactions').select('id,user_id,type,amount,account_id,category_id,subcategory_id,from_account_id,to_account_id,date,note,created_at,updated_at').eq('user_id',userId).order('date',{ascending:false}).order('created_at',{ascending:false});if(options.fromDate)q=q.gte('date',options.fromDate);if(options.toDate)q=q.lte('date',options.toDate);const[t,a,c,s]=await Promise.all([q,supabase.from('expense_accounts').select('id,name,type,currency,icon').eq('user_id',userId).order('name'),supabase.from('expense_categories').select('id,name,type,icon,color').eq('user_id',userId).eq('is_archived',false).order('type').order('name'),supabase.from('expense_subcategories').select('id,name,category_id').eq('user_id',userId).eq('is_archived',false).order('name')]);if(t.error)throw t.error;if(a.error)throw a.error;if(c.error)throw c.error;if(s.error)throw s.error;const accounts=(a.data??[]) as AccountRow[],categories=(c.data??[]) as CategoryRow[],subs=(s.data??[]) as SubRow[];const am=new Map(accounts.map(x=>[x.id,x])),cm=new Map(categories.map(x=>[x.id,x])),sm=new Map(subs.map(x=>[x.id,x]));const transactions=((t.data??[]) as TransactionRow[]).map(row=>({...row,amount:toMoney(row.amount),account_name:row.type==='transfer'?`${am.get(row.from_account_id??'')?.name??'Account'} → ${am.get(row.to_account_id??'')?.name??'Account'}`:am.get(row.account_id??'')?.name??null,account_currency:am.get(row.account_id??'')?.currency??am.get(row.from_account_id??'')?.currency??null,category_name:cm.get(row.category_id??'')?.name??null,category_icon:cm.get(row.category_id??'')?.icon??null,subcategory_name:sm.get(row.subcategory_id??'')?.name??null}));return{transactions,accounts,categories};}
export async function createExpenseTransaction(userId:string,input:ExpenseTransactionInput){const payload={user_id:userId,type:input.type,amount:input.amount,account_id:input.type==='transfer'?null:input.account_id??null,category_id:input.type==='transfer'?null:input.category_id??null,subcategory_id:input.type==='transfer'?null:input.subcategory_id??null,from_account_id:input.type==='transfer'?input.from_account_id??null:null,to_account_id:input.type==='transfer'?input.to_account_id??null:null,date:input.date,note:input.note?.trim()||null};const{data,error}=await supabase.from('expense_transactions').insert(payload).select('id').single();if(error)throw error;return data;}
export async function updateExpenseTransaction(userId:string,id:string,input:ExpenseTransactionInput){const payload={type:input.type,amount:input.amount,account_id:input.type==='transfer'?null:input.account_id??null,category_id:input.type==='transfer'?null:input.category_id??null,subcategory_id:input.type==='transfer'?null:input.subcategory_id??null,from_account_id:input.type==='transfer'?input.from_account_id??null:null,to_account_id:input.type==='transfer'?input.to_account_id??null:null,date:input.date,note:input.note?.trim()||null};const{data,error}=await supabase.from('expense_transactions').update(payload).eq('id',id).eq('user_id',userId).select('id').single();if(error)throw error;return data;}
export async function deleteExpenseTransaction(userId:string,id:string){const{error}=await supabase.from('expense_transactions').delete().eq('id',id).eq('user_id',userId);if(error)throw error;}
export async function createExpenseAccount(userId:string,input:{name:string;type:string;currency:string}){const{data,error}=await supabase.from('expense_accounts').insert({user_id:userId,name:input.name.trim(),type:input.type,currency:input.currency.toUpperCase()}).select('id,name,type,currency,icon').single();if(error)throw error;return data as AccountRow;}
export async function createExpenseCategory(userId:string,input:{name:string;type:'expense'|'income';icon?:string}){const{data,error}=await supabase.from('expense_categories').insert({user_id:userId,name:input.name.trim(),type:input.type,icon:input.icon?.trim()||null,is_default:false,is_archived:false}).select('id,name,type,icon,color').single();if(error)throw error;return data as CategoryRow;}
