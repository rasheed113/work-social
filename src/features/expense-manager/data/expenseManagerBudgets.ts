import {supabase} from '../../../lib/supabase/client';
import type {ExpenseBudgetInput,ExpenseBudgetRecord} from '../domain/budgets';
interface Row{id:string;category_id:string;subcategory_id:string|null;category_name:string;subcategory_name:string|null;category_icon:string|null;category_color:string|null;category_archived:boolean;budget_amount:unknown;period:'monthly';start_date:string;end_date:string;spent:unknown}
export async function loadExpenseBudgets():Promise<ExpenseBudgetRecord[]>{const{data,error}=await supabase.rpc('expense_manager_budget_progress');if(error)throw error;return((data??[]) as Row[]).map(r=>({id:String(r.id),category_id:String(r.category_id),subcategory_id:r.subcategory_id?String(r.subcategory_id):null,category_name:String(r.category_name),subcategory_name:r.subcategory_name==null?null:String(r.subcategory_name),category_icon:r.category_icon==null?null:String(r.category_icon),category_color:r.category_color==null?null:String(r.category_color),category_archived:Boolean(r.category_archived),budget_amount:Number(r.budget_amount)||0,period:r.period,start_date:String(r.start_date),end_date:String(r.end_date),spent:Number(r.spent)||0}));}
export async function createExpenseBudget(userId:string,input:ExpenseBudgetInput){const{error}=await supabase.from('expense_budgets').insert({user_id:userId,category_id:input.categoryId,subcategory_id:input.subcategoryId??null,amount:input.amount,period:input.period,start_date:input.startDate,end_date:input.endDate});if(error)throw error;}
export async function updateExpenseBudget(userId:string,id:string,input:ExpenseBudgetInput){const{error}=await supabase.from('expense_budgets').update({category_id:input.categoryId,subcategory_id:input.subcategoryId??null,amount:input.amount,period:input.period,start_date:input.startDate,end_date:input.endDate}).eq('id',id).eq('user_id',userId);if(error)throw error;}
export async function getExpenseBudgetByPeriod(userId:string,categoryId:string,startDate:string,endDate:string,excludeId?:string,subcategoryId?:string){let q=supabase.from('expense_budgets').select('id',{count:'exact',head:true}).eq('user_id',userId).eq('category_id',categoryId).eq('start_date',startDate).eq('end_date',endDate);if(subcategoryId)q=q.eq('subcategory_id',subcategoryId);else q=q.is('subcategory_id',null);if(excludeId)q=q.neq('id',excludeId);const{count,error}=await q;if(error)throw error;return(count??0)>0;}
