export type ExpenseBudgetPeriod='monthly';
export interface ExpenseBudgetRecord{id:string;category_id:string;subcategory_id:string|null;category_name:string;subcategory_name:string|null;category_icon:string|null;category_color:string|null;category_archived:boolean;budget_amount:number;period:ExpenseBudgetPeriod;start_date:string;end_date:string;spent:number;}
export interface ExpenseBudgetInput{categoryId:string;subcategoryId?:string;amount:number;period:ExpenseBudgetPeriod;startDate:string;endDate:string;}
export function monthBounds(month:string){if(!/^\d{4}-\d{2}$/.test(month))throw new Error('Choose a valid budget month.');const[y,m]=month.split('-').map(Number);if(!Number.isInteger(y)||!Number.isInteger(m)||m<1||m>12)throw new Error('Choose a valid budget month.');const end=new Date(Date.UTC(y,m,0));return{startDate:`${y}-${String(m).padStart(2,'0')}-01`,endDate:`${y}-${String(m).padStart(2,'0')}-${String(end.getUTCDate()).padStart(2,'0')}`};}
export function monthValue(d:string){return d.slice(0,7)}
export function validateBudgetInput(input:ExpenseBudgetInput){if(!input.categoryId)return'Choose an expense category.';if(!Number.isFinite(input.amount)||input.amount<=0)return'Budget limit must be greater than zero.';if(input.period!=='monthly')return'Monthly budgets are the supported budget period.';if(!/^\d{4}-\d{2}-\d{2}$/.test(input.startDate)||!/^\d{4}-\d{2}-\d{2}$/.test(input.endDate))return'Choose a valid budget month.';if(input.endDate<input.startDate)return'Budget end date must be after its start date.';return'';}
export function budgetRatio(spent:number,limit:number){return limit<=0?0:spent/limit}export function budgetRemaining(spent:number,limit:number){return limit-spent}export function budgetState(spent:number,limit:number):'Healthy'|'Approaching Limit'|'Exceeded'{if(limit<=0||spent>limit)return'Exceeded';if(spent/limit>=.8)return'Approaching Limit';return'Healthy'}
